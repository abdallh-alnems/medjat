# طرق تسجيل الحضور في Medjat — وأين تعمل كل طريقة

> آخر تحديث: 2026-08-18
> المرجع الوحيد للحقيقة في الكود: `backend_medjet/core/AttendanceMethodResolver.php`
> ونسخة قاعدة البيانات: عمودا `attendance.check_in_method` / `check_out_method`.

هذا الملف يجيب على سؤالين فقط: **ما هي الطرق الموجودة فعلاً؟** و**من أي منصة تُنفَّذ كل
واحدة منها؟** — لا يشرح تصميم كل ميزة (لذلك ارجع إلى `specs/` وإلى تعليقات الملفات نفسها).

---

## 1. الخلاصة في جدول واحد

| الطريقة | تطبيق الموظف `medjat_app` (Android + iOS) | متصفّح الموظف `/me` | تطبيق الإدارة `medjat_central` (Android + iOS) | ويب الإدارة `medjat_central_web` + الديسكتوب | الكشك `medjat_kiosk` (تابلت Android) | جهاز بصمة ZKTeco |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| `qr_gps` — QR + موقع | ✅ | ➖ | ⚙️ ملصق QR الثابت | ⚙️ شاشة QR المتغيّر | ➖ | ➖ |
| `gps_only` — موقع فقط | ✅ | ✅ | ➖ | ➖ | ➖ | ➖ |
| `face_selfie` — سيلفي مُطابَق | ✅ | ➖ | ⚙️ العتبة والوضع | ⚙️ العتبة والوضع | ➖ (له مساره الخاص) | ➖ |
| `photo_gps` — صورة كدليل | ✅ | ⚠️ صورة نعم، الطريقة لا | ⚙️ | ⚙️ | ➖ | ➖ |
| `wifi_gps` — شبكة + موقع | ✅ (BSSID على Android، IP على iOS) | ➖ | ⚙️ اعتماد الشبكات | ⚙️ اعتماد الشبكات | ➖ | ➖ |
| `crew_gps` — مشرف يسجّل لطاقمه | ✅ (المشرف فقط) | ➖ | ⚙️ ربط المشرف بالطاقم | ⚙️ ربط المشرف بالطاقم | ➖ | ➖ |
| `kiosk` — تابلت مشترك بالفرع | ➖ | ➖ | ⚙️ إدارة الأكشاك | ⚙️ إدارة الأكشاك | ✅ | ➖ |
| `device` — جهاز بصمة | ➖ | ➖ | ⚙️ إدارة الأجهزة | ⚙️ استيراد البصمات | ➖ | ✅ |
| `manual` — إدخال إداري | ➖ | ➖ | ✅ | ✅ | ➖ | ➖ |

**دلالة الرموز:** ✅ الحضور يُسجَّل فعلياً من هنا · ⚙️ لا يُسجَّل هنا، لكن هذه هي المنصة التي
تُعِدّ الطريقة أو تشغّل ما تحتاجه · ➖ غير مدعومة · ⚠️ انظر التفصيل.

**منصات لا تُسجِّل حضوراً إطلاقاً:** `medjat_admin` (لوحة السوبر-أدمن) — تعرض طرق الشركة
للتشخيص فقط (`lib/data/model/tenant_diagnostics_model.dart`)، ولا تكتب أي حضور.

---

## 2. كيف تُحسم الطريقة لموظف بعينه

`AttendanceMethodResolver::resolveForEmployee()` — **الأخصّ يفوز**، و`NULL` معناها «ورّث
من المستوى التالي»:

```
1) تجاوز على الموظف        employees.attendance_methods
2) اتحاد فئات الموظف       employee_categories.attendance_methods  (UNION عبر كل فئاته)
3) تجاوز على الفرع         branches.attendance_methods
4) إعداد الشركة            tenants.attendance_methods
   ← وإن كان الكل فارغاً: ['qr_gps']
```

قائمتان ثابتتان في نفس الملف:

- `ALLOWED` — كل القيم الصالحة: `qr_gps`, `gps_only`, `face_selfie`, `photo_gps`,
  `wifi_gps`, `crew_gps`, `device`, `manual`, `kiosk`.
- `SELF_SERVICE` — ما يستطيع الموظف تسجيله بنفسه من هاتف أو متصفح:
  `qr_gps`, `gps_only`, `face_selfie`, `photo_gps`, `wifi_gps`.
  (استُخرجت في ثابت واحد تحديداً كي لا يفترق `check_in.php` عن `check_out.php`.)

`crew_gps` تُحلّ **للمشرف** لا للمُسجَّل له: وجودها تعني «يجوز لي أن أسجّل لطاقمي».
`device` لا يستشير هذا الحلّال أصلاً (الجهاز يفوّض نفسه بالسيريال + ربط User ID)، بينما
`kiosk` **تستشيره**، حتى تستطيع شركة أن تضع عمّال المصنع على الكشك وموظفي المكتب على `gps_only`.

### قيم موجودة في قاعدة البيانات فقط

لا تُختار من أي شاشة، لكنها تظهر في التقارير:

| القيمة | معناها |
|---|---|
| `offline` | مزامنة حضور سُجِّل بلا شبكة من تطبيق الموظف — `app/attendance/sync_offline.php` |
| `auto` | انصراف آلي أنهى يوماً مفتوحاً (في `check_out_method` فقط) |
| `qr_gps_face` | قيمة قديمة بلا مسار كود؛ باقية لأن صفوفاً قديمة تحملها |

---

## 3. الطرق واحدة واحدة

### `qr_gps` — مسح QR داخل نطاق الفرع

- **من أين تُسجَّل:** تطبيق الموظف فقط — `lib/view/screen/attendance/scan_qr_screen.dart`.
  المتصفّح لا يمسح QR ولا يُرسل `method`، فالخادم يستنتج `gps_only`.
- **نكهتان لكل فرع على حدة:**
  - **QR مطبوع ثابت:** يُقارَن بـ`branches.qr_code`. الملصق يُولَّد من تطبيق الإدارة —
    `frontend/mobile/manager/lib/view/screen/branch/branch_qr_poster_screen.dart`.
  - **QR متغيّر (rotating):** يُفعَّل بـ`branches.rotating_qr_enabled`، الكود يتغيّر كل
    عدة ثوانٍ ويُستهلك مرة واحدة لكل موظف — `models/BranchQrChallengeModel.php`.
    الشاشة التي تُعلَّق على الباب موجودة **على الويب فقط**:
    `frontend/web/manager/src/app/(app)/attendance/branch-qr/page.tsx`.
- **مهم:** لا إصدار تطبيق جديد بين النكهتين — التطبيق يمرّر ما قرأته الكاميرا كما هو،
  والخادم وحده يقرّر أيّهما يتوقّع.
- **يفرضه الخادم دائماً:** الـGPS مطلوب مع QR (لا يمرّ كود بلا موقع حقيقي).

### `gps_only` — الموقع وحده

- **من أين تُسجَّل:** تطبيق الموظف (`gps_check_in_screen.dart`) **و** متصفّح الموظف.
- **هي الطريقة الوحيدة التي يستطيع المتصفّح إنتاجها**: صفحة `/me/attendance` ترسل
  `branch_id` + `latitude` + `longitude` بلا `method`، فيستنتجها `check_in.php`.
  نتيجة ذلك عملياً: شركة لا تُفعّل `gps_only` لا يستطيع موظفوها الحضور من المتصفّح.
- `0,0` (رفض إذن الموقع) مرفوضة صراحةً بـ`LOCATION_REQUIRED`.

### `face_selfie` — سيلفي يُطابقه الخادم

- **من أين تُسجَّل:** تطبيق الموظف فقط — `face_check_in_screen.dart` (+ `face_enroll_screen.dart`
  للتسجيل الأول).
- **لماذا لا المتصفّح:** النموذج (`assets/models/mobilefacenet.tflite`) يعمل على الجهاز؛
  المتصفّح لا يستطيع تشغيله.
- **قاعدة ثابتة:** الهاتف يستخرج البصمة الرقمية للوجه، و**الخادم** هو من يُعطي الدرجة
  (`core/FaceMatchService.php`) مقابل nonce يُستهلك مرة واحدة في `face_challenges`.
  حكم العميل لا يُصدَّق أبداً.
- الشركات تبدأ على `face_enforce_mode = 'log_only'` (تسجيل في `face_verification_logs`
  دون رفض أحد) ثم تنتقل إلى `enforce` بعد ضبط العتبة على بيانات حقيقية.
- الانصراف يُطابَق أيضاً حين تكون `face_selfie` هي الطريقة الذاتية الوحيدة للموظف —
  وإلا لظلّت الطريقة مطبَّقة نصف تطبيق (`check_out.php`).

### `photo_gps` — موقع + صورة كدليل لا كقياس حيوي

- **من أين تُسجَّل:** تطبيق الموظف — `photo_check_in_screen.dart`.
- **لا شيء يُقيّم الصورة**، ولا يُقبَل بصمة حضور أو يُرفض بسببها. وُجدت كي تحصل الشركة على
  رادع دون أن تدخل في التزامات المعالجة البيومترية (قانون 14/2025) التي تفرضها `face_selfie`.
- **⚠️ فرق مهم عن المتصفّح:** المتصفّح **يلتقط صورة** حين تطلبها الشركة، لكن ذلك قاعدة
  قناة لا طريقة: `tenants.web_attendance_photo_required` (مفعّلة افتراضياً مع تفعيل
  الحضور من المتصفّح). `check_in.php` يدمج السببين بـ OR لا بـ branch، لأن شركة قد تكون
  على الاثنين معاً. أي: صورة من المتصفّح ≠ `photo_gps`.
- الصورة تُخزَّن قبل كتابة البصمة (`core/PunchPhotoService.php`)، فلا يبقى حضور بلا صورة
  في شركة اشترطتها.

### `wifi_gps` — الشبكة قيد فوق السياج الجغرافي

- **من أين تُسجَّل:** تطبيق الموظف فقط.
- **الفرق بين المنصّتين داخل التطبيق نفسه:**
  - **Android:** يقرأ الـBSSID ويُرسله — `lib/core/services/network_service.dart`.
  - **iOS:** يحتاج `com.apple.developer.networking.wifi-info` وهو غير مطلوب من Apple،
    فيسقط إلى مطابقة **IP** (`ip_v4` / `ip_cidr` في `branch_networks`).
- **قاعدة لا تتغيّر:** الشبكة **قيد إضافي فوق** السياج الجغرافي، وليست بديلاً عنه — الـGPS
  ينحرف داخل المباني، وإشارة الواي-فاي تتسرّب خارجها (`core/NetworkVerifier.php`).
- وضع التعلّم يكتشف شبكات الفرع تلقائياً في `branch_networks`؛ الراوتر الواحد يُظهر عادةً
  أكثر من BSSID (2.4/5 GHz)، فاعتماد بعضها فقط يحبس الناس خارج الحضور.
- «المشاهدة» تُسجَّل قبل حكم الـGPS وبغضّ النظر عنه، لأن «حاول من خارج النطاق على شبكة X»
  هي بالضبط الإشارة التي تحتاجها شاشة الاعتماد.

### `crew_gps` — مشرف يسجّل لمن معه في الموقع

- **من أين تُسجَّل:** تطبيق الموظف، شاشة الطاقم — `lib/view/screen/attendance/crew_screen.dart`
  (تظهر فقط إن كان الموظف مشرفاً على أحد).
- **الخادم:** `app/attendance/crew_check_in.php` — وهو **المكان الوحيد** الذي يتصرّف فيه
  اعتماد موظف نيابةً عن غيره، ولذلك فُصل عن `check_in.php` بدل أن يكون فرعاً بداخله.
  الضوابط الأربعة كلها على الخادم: المشرف من التوكن لا من الجسم، والهدف قابل للكتابة فقط
  إن كان `crew_supervisor_id` يساوي المشرف، والدفعة تُرفض كاملة إن سقط أي هدف، وكل صف
  يُسجّل من كتبه.
- إحداثية واحدة تُؤخذ من هاتف المشرف وتُكتب على كل صفوف الدفعة — وهذا هو الشكل الأمين
  للدليل: «مَن سجّل كان في الموقع»، لا «كلٌّ من هؤلاء حُدِّد موقعه على حدة».
- **أين تُضبَط:** شاشة طرق الحضور على الويب (أُضيفت 2026-08-25 — وهي المكان الوحيد في
  المنتج الذي يضبطها)، أو كتجاوز على مستوى الفئة/الموظف عبر `set_method_override.php`.
  الشكل الطبيعي `['gps_only','crew_gps']` على فئة «المشرفين»: تجاوز الموظف **يستبدل** ولا
  يضيف، فوضع `crew_gps` وحدها على مشرف يُفقده القدرة على تسجيل حضوره هو.
- **كانت تُمحى بصمت** من إعداد الشركة عند أي حفظ من تطبيق الإدارة حتى 2026-08-25 —
  التفصيل والإصلاح في **القسم 6.3**.

### `kiosk` — تابلت مشترك عند باب الفرع

- **من أين تُسجَّل:** تطبيق `medjat_kiosk` — **Android فقط** (لا يوجد مجلّد iOS في المشروع).
- **ليست خدمة ذاتية:** الموظف لا يحمل اعتماداً، والتابلت يفوّض نفسه بتوكن مربوط بالفرع
  (`Auth::authenticateKiosk`).
- **مساران للتعرّف:**
  - **الوجه** — `app/kiosk/identify.php`: التابلت يُرسل البصمة الرقمية **لا الحكم**؛
    الخادم يطابقها على كشف الفرع. الفشل يرجع **200 مع `outcome`** لا 4xx، لأنه نتيجة
    طبيعية لتفاعل طبيعي.
  - **الكود الشخصي** — `app/kiosk/identify_by_code.php`: مسار احتياطي (كمامة، ضمادة،
    لمبة محترقة فوق التابلت). أضعف عمداً ومُعلَّم كذلك، والشركة ممنوعة من التشغيل على
    الأكواد وحدها.
- كلا المسارين يُرجعان `punch_ticket` قصير العمر؛ `app/kiosk/punch.php` يستبدل التذكرة
  ولا يقبل `employee_id` من الطلب، وإلا لأمكن للتابلت أن يتعرّف على شخص ويبصم باسم آخر.
- يُكتب في قاعدة البيانات كـ`kiosk`، مع تمييز `station_face` / `station_code` في سجلّ التعرّف.
- **الإدارة:** `frontend/mobile/manager/lib/view/screen/kiosk/` و`frontend/web/manager/src/app/(app)/kiosks`.

### `device` — جهاز بصمة ZKTeco

- **من أين تُسجَّل:** الجهاز نفسه. لا يمرّ بأي تطبيق.
- **المسار:** الجهاز يتّصل خارجاً إلى `backend_medjet/device/iclock.php` عبر **HTTP عادي
  على المنفذ 8090 مباشرةً إلى الأصل** — البرامج الثابتة القديمة لا تدعم TLS جيداً ولا ترسل
  SNI، فلا تستطيع المرور عبر Cloudflare. **كل ردّ يجب أن يكون 200 نصاً عادياً** وإلا
  أعاد الجهاز الإرسال إلى الأبد.
- الاستيعاب في `core/DevicePunchIngestor.php` (لا يُجهض دفعة أبداً)، والكتابة عبر
  `AttendanceModel` كـ`check_in_method = 'device'`.
- **مسار ثانٍ:** استيراد ملف بصمات يدوياً — `app/devices/import_punches.php`، وواجهته على
  الويب `src/app/(app)/settings/import-punches/page.tsx`. وتطبيق الديسكتوب يستطيع **قراءة
  الجهاز مباشرة عبر الشبكة المحلية** (`frontend/desktop/manager/src/device.js`) ثم تغذية
  نفس الصفحة — وهي القدرة الوحيدة التي يملكها الديسكتوب ولا يملكها المتصفّح.
- **الإدارة:** `frontend/mobile/manager/lib/view/screen/devices/` (تسجيل الجهاز وربط المستخدمين).

### `manual` — إدخال إداري

- **من أين تُسجَّل:** تطبيق الإدارة `medjat_central` **و** ويب الإدارة (والديسكتوب معه).
- **الخادم:** `app/attendance/manual_check_in.php` — يتطلّب صلاحية `manage_attendance`،
  ويرفض إن لم تكن `manual` ضمن طرق الشركة، ويحصر المُدخِلين في
  `manual_attendance_admin_ids` إن ضُبطت.

---

## 4. المنصات — ماذا تدعم كل واحدة

### `medjat_app` — تطبيق الموظف (Android + iOS، وAppGallery)

المنصّة الوحيدة الكاملة: `qr_gps`, `gps_only`, `face_selfie`, `photo_gps`, `wifi_gps`,
`crew_gps` — إضافة إلى المزامنة دون اتصال (`offline`).
الخادم يُرجع الطرق الفعّالة في إعداد الحضور، والتطبيق يعرض ورقة اختيار الطريقة حين تتوفّر
أكثر من واحدة (`lib/view/screen/home/widgets/attendance_method_picker_sheet.dart`).

ضابطان إضافيان يعملان فوق أي طريقة في هذا التطبيق:

- **قفل الجهاز البيومتري** (`tenants` → `requiresLocalBiometric`): يجيب على السؤال الذي
  تفترضه كل الضوابط الأخرى — هل الطارق هو صاحب الهاتف؟ اختياري لكل شركة، وأرخص فحص في
  المسار فيُرفض قبل أي عمل على QR أو GPS أو الوجه.
- **رفض الموقع المزيّف** (`tenants.reject_mock_location`): **Android فقط** — iOS لا يبلّغ
  عنه إطلاقاً. كل حظر أو وسم يُكتب في `attendance_security_logs`.
  الأجهزة المروَّتة **لا تُحظر عمداً** (شائعة على الأجهزة الرخيصة، وليست دليل تحايل).

### متصفّح الموظف — `frontend/web/manager/src/app/(employee)/me/*`

- الصفحات: `/me/activate` (تفعيل أول + اختيار PIN)، `/me/login`، `/me/attendance`.
- **الطريقة الوحيدة: `gps_only`** — بالاستنتاج، لأن الصفحة لا ترسل `method` ولا `qr_code`.
- تلتقط صورة حين تطلبها الشركة (`photo_required` من `web_status.php`).
- القناة تُستخرج من الجلسة لا من جسم الطلب، حتى لا تتنكّر بصمة متصفّح في هيئة بصمة تطبيق.
- ضوابط تعويضية: تفعيل صريح لكل شركة (`web_attendance_enabled`، **مغلقة افتراضياً**)،
  استثناء على مستوى الفئة الوظيفية (`WebAccessPolicy`)، جلسة واحدة نشطة لكل موظف،
  وكشف الجهاز المشترك (`SharedDeviceDetector` — يَسِم ولا يحظر).
- **قيد الشبكة:** لا قراءة للـBSSID أصلاً — الضابط الوحيد الممكن هنا هو مطابقة الـIP،
  ويُنفَّذ منذ 2026-08-25 عبر `NetworkVerifier::verifyBrowser()` في `check_in.php` و
  `check_out.php`. قبل ذلك كان `network_constraint` **إعلامياً فقط** (القسم 6.2).

### `medjat_central` — تطبيق الإدارة (Android + iOS)

لا يسجّل حضوراً ذاتياً. يُسجّل `manual`، ويُعِدّ الباقي: شاشة طرق الحضور، ملصق QR للفرع،
إدارة الأجهزة، إدارة الأكشاك، اعتماد شبكات الفرع.

### `medjat_central_web` + `medjat_central_desktop`

نفس صلاحيات تطبيق الإدارة على الويب، وتزيد بشيئين لا وجود لهما في تطبيق الإدارة:
**شاشة الـQR المتغيّر** على الباب، و**قراءة جهاز البصمة عبر الشبكة المحلية** (الديسكتوب فقط).
الديسكتوب غلاف Electron فوق نفس الويب، فكل ما ينطبق على الويب ينطبق عليه.

### `medjat_kiosk` — تابلت الفرع (Android فقط)

الطريقة `kiosk` وحدها. لا يوجد على الجهاز أي اعتماد لموظف بعينه.

### جهاز ZKTeco

الطريقة `device` وحدها، عبر `device/iclock.php` (تفاصيل البروتوكول في `device/README.md`).

### `medjat_admin`

لا يكتب حضوراً. يعرض طرق الشركة للتشخيص فقط.

---

## 5. أين تُضبَط الطرق

| المستوى | العمود | الشاشة |
|---|---|---|
| الشركة | `tenants.attendance_methods` | `medjat_central` → إعدادات → طرق الحضور · و`frontend/web/manager/src/app/(app)/settings/attendance-method` |
| الفرع | `branches.attendance_methods` | نفس الشاشتين (تبويب الفروع) |
| الفئة الوظيفية | `employee_categories.attendance_methods` | نفس الشاشتين (تبويب الفئات) → `app/attendance/set_method_override.php` |
| الموظف | `employees.attendance_methods` | نفس الشاشتين (تبويب الموظفين) → نفس النقطة |

**ما لا يظهر في قائمة الاختيار — والفرق بين الشاشتين ليس تجميلياً:**

| | ويب الإدارة | تطبيق الإدارة `medjat_central` |
|---|---|---|
| الملف | `settings/attendance-method/page.tsx` (ثابت `METHODS`) | `attendance_method_controller.dart` (ثابت `allMethods`) |
| طبيعة القائمة | **عرض** — `toggleMethod` يعدّل ما أرسله الخادم | **مِصفاة** على رد الخادم عند التحميل |
| الطريقة الغائبة عنها | تُخفى ولا تُمسّ | تُخفى — ولا تُمسّ منذ 2026-08-25 |
| الغائب اليوم | `device`, `kiosk` | لا شيء — تُطابق `ALLOWED` |

الويب يحذف `device` و`kiosk` عمداً (ليست خدمة ذاتية، ولكلٍّ شاشتها).

حتى 2026-08-25 كانت قائمة تطبيق الإدارة **مدمِّرة**: الطريقة الغائبة عنها تُحذف من إعداد
الشركة عند أول حفظ. لم تعد كذلك — لا لأن القائمة اكتملت (وقد اكتملت)، بل لأن الحفظ لم يعد
يحمل الطرق إلا حين تكون هي موضوع الحفظ. انظر **القسم 6.3**.

**تجاوزات الفرع/الفئة/الموظف** في تطبيق الإدارة تعرض خمس طرق فقط
(`qr_gps`, `gps_only`, `face_selfie`, `wifi_gps`, `manual`) — أي أن `photo_gps` و`crew_gps`
لا يمكن ضبطهما من هناك. لكنها **غير مدمِّرة**: الحوار يُهيَّأ من قائمة التجاوز الحقيقية،
فالقيم غير المعروضة تبقى كما هي.

---

## 6. ثلاث مشاكل وُجدت وأُصلحت (2026-08-25)

> الحالة: كل ما يلي **مُصلَح في الريبو** وينتظر `deploy.sh`. الوصف باقٍ لأن سببَ
> كل مشكلة أهم من إصلاحها.

### 6.1 متصفّح الموظف يدعم `gps_only` وحدها — بالاستنتاج لا بالاختيار

`checkIn()` في `features/employee-attendance/api.ts` ترسل `branch_id` و`latitude` و
`longitude` و`photo_base64` فقط — **لا `method` ولا `qr_code`**. وفي `check_in.php:46`:

```php
$requestedMethod = $input['method'] ?? ($qrCode ? 'qr_gps' : 'gps_only');
```

فالمتصفّح يقع دائماً على `gps_only`، ثم يُقارَن بطرق الموظف الفعّالة (السطر 51).
النتيجة: **شركة لا تُفعّل `gps_only` لا يستطيع موظفوها الحضور من المتصفّح** ولو كان
`web_attendance_enabled = 1`. والرسالة التي تصل تعتمد على إعدادها:

- طرقها `qr_gps` → `400 QR_REQUIRED` («امسح كود الفرع») — وهو ما لا يستطيعه المتصفّح.
- طرقها `face_selfie` أو `wifi_gps` أو `photo_gps` → `403 METHOD_NOT_ALLOWED`.

أي أن مفتاح الحضور من المتصفّح **يشترط ضمناً تفعيل `gps_only`**، وكان هذا الاشتراط غير
مذكور في أي شاشة إعداد.

**الإصلاح** — لم يُغيَّر الاشتراط (المتصفّح فعلاً لا يمسح QR ولا يشغّل نموذج الوجه)، بل
أُظهر في الطرفين قبل أن يصطدم به أحد:

- `web_status.php` صار يُرجع `can_punch` و`blocked_reason`، محسوبَين من
  `AttendanceMethodResolver::resolveForEmployee`.
- صفحة `/me/attendance` تُخفي زر الحضور وتشرح السبب بدل أن تعطي زراً مضمون الفشل.
- **الانصراف لا يُحجب أبداً**: من سجّل حضوره من التطبيق يجب أن يقدر على إغلاق يومه من
  المتصفّح، و`check_out.php` بلا بوّابة طريقة لهذا السبب بالذات.
- `company.php` يُرجع `web_requires_gps_only`، وبطاقة الحضور من المتصفّح تعرض تحذيراً
  أحمر فوق قائمة القيود — لأن هذه ليست «ميزة أضعف» بل «لا تعمل إطلاقاً».

### 6.2 `network_constraint` في `web_status.php` إعلامي فقط — لا يُنفَّذ على مسار البصمة

`web_status.php:59-70` يستعلم عن صفوف `ip_v4` / `ip_cidr` في `branch_networks` ويرجع
`network_constraint: 'ip' | 'none'`. لكن `NetworkVerifier::verify()` له **موضعان فقط** في
الباك-إند بأكمله:

```
check_in.php:149   داخل  if ($requestedMethod === 'wifi_gps')
check_out.php:126  داخل  if (... && $input['method'] === 'wifi_gps')
```

والمتصفّح لا يرسل `wifi_gps` أبداً (انظر 6.1) — فلا يمرّ بأي منهما. كذلك
`src/app/api/employee/[...path]/route.ts` وسيط تمرير لا يفحص شبكة.

يعني: **تقييد الشبكة المذكور في spec 004 كضابط تعويضي للقناة الأضعف غير مطبَّق على بصمة
المتصفّح.** والقيمة نفسها غير مستهلكة في الواجهة — `grep` لا يجد `network_constraint` إلا
في تعريف النوع (`api.ts:65`)، فلا صفحة تقرؤها. أي أن الضابط غائب والإشارة إليه غير معروضة.

بقية الضوابط التعويضية **تعمل** فعلاً: المفتاح لكل شركة، واستثناء الفئة، والجلسة الواحدة،
وكشف الجهاز المشترك، والصورة.

**الإصلاح** — `NetworkVerifier::verifyBrowser()` جديدة، و`check_in.php` و`check_out.php`
يستدعيانها عند `$isWeb` بعد اجتياز السياج الجغرافي.

ولماذا دالة مستقلة لا علَم داخل `verify()`: `wifi_match` الافتراضي `bssid`، والمتصفّح لا
يُنتج BSSID أبداً — فاستدعاء `verify()` هنا كان **سيرفض كل بصمة متصفّح** في أي فرع على
وضع `enforcing`. أي انقطاع خدمة يرتدي زيّ ضابط أمني. فالدالة الجديدة تُطبّق صفوف الـIP
وحدها، وتحافظ على نفس تسلسل الرحمة الموجود في `verify()`:

| الحالة | النتيجة |
|---|---|
| `wifi_mode` = NULL أو `learning` | يمرّ — الفرع ما زال يجمع شبكاته |
| لا صفوف IP معتمدة | يمرّ — سوء إعداد لا سبب لحبس الجميع |
| الـIP مطابق | يمرّ |
| `wifi_mode` = `optional` | يمرّ — لتغطية من هو على بيانات الهاتف |
| غير ذلك | **يُرفَض** ويُسجَّل بـ`web_wrong_network` |

و`web_status.php` صار يستدعي `NetworkVerifier::browserConstraint()` بدل استعلامه الخاص،
فالمُعلَن والمُنفَّذ من مصدر واحد ولا يفترقان مجدداً.

القيمة `web_wrong_network` تحتاج توسيع الـenum: `migrations/2026_08_25_web_network_refusal_reason.sql`
— **قيمه مقروءة من الإنتاج مباشرةً** (19 قيمة) لا من آخر مِيغريشن، عملاً بالتحذير المكتوب في
`2026_08_06_restore_web_security_log_reasons.sql`. وقيمة مستقلّة عن `wrong_network` الخاصة
بالتطبيق عمداً: التطبيق طُوبق على نقطة الوصول، والمتصفّح على عنوان IP — ومن يقرأ هذه
الصفوف ليضبط قائمة فرع يحتاج التمييز بينهما.

### 6.3 قائمة `allMethods` في تطبيق الإدارة تمسح `crew_gps` من إعداد الشركة

هذه ليست ميزة ناقصة بل **فقدان بيانات**، وسلسلتها ثلاث خطوات في
`attendance_method_controller.dart`:

```dart
// 53  القائمة — لا crew_gps فيها
static const allMethods = ['qr_gps','gps_only','face_selfie','photo_gps',
                           'wifi_gps','device','manual','kiosk'];

// 93  التحميل: كل ما ليس في القائمة يسقط بصمت
tenantMethods = (data['attendance_methods'] as List)
    .map((e) => e.toString()).where(allMethods.contains).toSet();

// 174 الحفظ: المُصفّى يُعاد إرساله كما هو
methods: tenantMethods.toList(),
```

والحفظ **ليس محصوراً بمفتاح الطرق**: نفس السطر `methods: tenantMethods.toList()` يُرسَل من
ستة مسارات — تبديل طريقة، الحضور دون اتصال، رفض الموقع المزيّف، القفل البيومتري المحلي،
حفظ مُدخِلي الحضور اليدوي. فأي لمسة على تلك الشاشة تكفي.

**التعليق فوق القائمة يوثّق أن هذا حدث من قبل:** `kiosk` كانت ناقصة حتى 2026-08-06، «وأي
شركة تشغّل أكشاكاً كانت ستفقدها عند أول تبديل». `crew_gps` أُضيفت للباك-إند في
`2026_08_06_crew_attendance.sql` ولم تُضف للقائمة — نفس الخطأ، نفس اليوم تقريباً.

**الويب لا يعاني منه** لأن `METHODS` هناك عرض لا مِصفاة (`toggleMethod` يعدّل ما أرسله
الخادم)، و**تجاوزات** الفرع/الفئة/الموظف في تطبيق الإدارة سليمة أيضاً لأنها تُهيَّأ من
القائمة الحقيقية. الضرر محصور في **الطرق على مستوى الشركة من تطبيق الإدارة**.

**الإصلاح** — على مستويين، لأن السطر الواحد يعالج العرَض لا العلّة:

1. `'crew_gps'` أُضيفت إلى `allMethods` (والقائمة صارت تُطابق `ALLOWED`).
2. **والأهم:** `methods` صار **اختيارياً** في `updateAttendanceConfig`، ولا يُرسَل إلا من
   `toggleTenantMethod` وحده. المسارات الخمسة الأخرى لم تعد تحمل قائمة الطرق أصلاً، فتصل
   إلى فرع `elseif` الآمن في الخادم الذي يقرأ الطرق من قاعدة البيانات ويمرّرها كما هي.
   بعد هذا لا تستطيع `allMethods` أن تقرّر ما تحتفظ به شركة، مهما نقصت.

وتبعٌ لازم: `saveManualAdminIds(null)` تعني «بلا تقييد — أي مسؤول يستطيع»، و`null` لا
تُميَّز عن «لم يُمرَّر» في معامل Dart اختياري، كما أن `isset()` في PHP لا تراها. فأُفردت
لها `updateManualAdminIds()` تُرسل المفتاح صراحةً، وتحوّل الخادم إلى
`array_key_exists`، مع إبقاء شرط «`manual` مفعّلة» على **الضبط** دون **المسح** — وإلا
عجزت شركة عن التنظيف بعد إطفاء الحضور اليدوي.

وأُضيفت `crew_gps` إلى قائمة العرض في الويب: هذه الشاشة هي المكان الوحيد في المنتج الذي
يستطيع ضبطها، وتركها خارجها كان يعني أنها غير قابلة للضبط من أي مكان. كما صار النوع
`AttendanceMethod` يعكس `ALLOWED` كاملة (تسع قيم) بدل ستّ — نوعٌ ينقصه ما يرسله الخادم
يجعل بحث الترجمة خاطئاً بصمت عند الشركة التي تستخدمه فعلاً.

---

## 7. المبادئ التي لا تُخالَف

1. **لا يُصدَّق حكم العميل أبداً.** الهاتف يستخرج، والخادم يُقيّم — في الوجه وفي الكشك على
   السواء. الفرق أن اعتماد الكشك، لو زُوِّر، لَزوّر لأي شخص في الفرع لا لصاحبه فقط.
2. **الواي-فاي قيد فوق السياج الجغرافي، لا بديل عنه.**
3. **القناة تأتي من الجلسة لا من جسم الطلب.**
4. **الوقت لكل شركة على حدة:** كل ختم زمني يمرّ عبر `core/TenantClock.php`، لا `date()` ولا
   `NOW()` مباشرة (PHP على UTC وMySQL على توقيت الخادم، فيختلفان بساعات).
5. **كل حظر يُكتب** في `attendance_security_logs`. المحاولة المحظورة التي لا تترك أثراً هي
   بالضبط ما أُنشئ هذا الجدول لإنهائه.
6. **كل القنوات تكتب عبر `AttendanceModel`** لا بـINSERT موازٍ — لأن هذه الدوال هي التي
   تحسب `late_minutes` و`worked_minutes` و`overtime_minutes` والحالة مقابل وردية الموظف؛
   أي إدخال بديل ينحرف بصمت ويظهر بعد شهور كرواتب لا تتطابق.
