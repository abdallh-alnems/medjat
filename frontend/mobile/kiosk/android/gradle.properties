# 3G/1G, matching permedjat_app and permedjat_central. The template's 8G/4G asks for
# more than this 8 GB machine has and makes R8 die of a Metaspace OOM on a
# release build rather than making anything faster.
org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
