# مشاكل مفتوحة وحلولها — الحضور والانصراف

> كل مشكلة اتقالت في شغل 2026-08-01/02، مع حلّها. تاريخ: 2026-08-02.
> كل بند **متحقَّق منه من الكود أو من القاعدة الحيّة** — الأمر المستخدم مكتوب جنب كل حقيقة.

---

## أ) اتقفلت خلاص ✅

| # | المشكلة | الحل اللي اتنفّذ |
|---|---|---|
| أ-1 | `mobilefacenet.tflite` مش في المستودع → الوجه ما يشتغلش | اتضاف (BSD-3، إشعار الرخصة جنبه). الجراف متحقَّق منه `[1,112,112,3]`→`[1,192]`، وبيتحزم في `flutter_assets` |
| أ-2 | العتبة 0.650 بترفض 52% من المحاولات الصحيحة | اتغيّرت لـ **0.450** — الثابت + `DEFAULT` العمود + الست شركات، منشور ومتحقَّق منه على السيرفر |
| أ-3 | ZKTeco مبني ومش منشور | `nginx-devices.conf` + `limit_req_zone` في `conf.d` + `ufw allow 8090/tcp`. متحقَّق من بره: `/iclock/cdata` → 200 نص، `/` → 404 |
| أ-4 | تناقض المواصفة: الجلسة تقفل عند الانصراف بس كود التفعيل أحادي الاستخدام | PIN من 6 أرقام (`research.md` R-002) + `FR-002c/d` |

---

## ب) مفتوحة — وليها حل محدد

### ب-1 ⚠️ ثغرة حقيقية: السيرفر بيصدّق العميل في الوجه

**الحالة:** موجودة في الكود المنشور دلوقتي.

`core/FaceMatchService.php` بياخد من العميل:

```php
$livenessPassed = !empty($input['liveness_passed']);      // بوليان من العميل
$candidate = self::parseEmbedding($input['face_embedding'] ?? null);  // 192 رقم من العميل
```

**السيرفر ما بيشوفش الصورة أبدًا.** فبيلد معدّل يقدر:

```
يطلب nonce جديد
   ↓
يبعت الـ embedding بتاعه (اتصوّر مرة واحدة قانونيًا واتخزّن)
   ↓
يبعت liveness_passed: true
   ↓
مقبول — والكاميرا ما فتحتش
```

الـ nonce بيمنع إعادة نفس **الطلب**، مش إعادة استخدام نفس **الأرقام** مع nonce جديد.

فالتوثيق بيقول *"لا تصدّق حكم العميل"* — وده صح في نص واحد فقط: السيرفر بيحسب التشابه بنفسه ✅. لكنه **بيصدّق** إن الأرقام دي جات من صورة اتاخدت دلوقتي، و**بيصدّق** إن كشف الحياة عدّى.

#### الحل — طبقتين، الاتنين رخاص

**(1) كشف التكرار على السيرفر — الأرخص وبيمسك الهجوم ده بالذات**

بصمة الوش الحقيقية **ما بتتكررش حرفيًا**. كاميرا حقيقية + إضاءة + ميل الرأس بيدّوا أرقام مختلفة كل مرة. فالـ embedding اللي بيتبعت **مطابق تمامًا** لمحاولة سابقة = مستحيل من كاميرا.

```
خزّن hash آخر N محاولة لكل موظف في face_verification_logs
لو التشابه مع محاولة سابقة للموظف نفسه > 0.9995
   → علّمها replayed_embedding في attendance_security_logs
```

- شغل سيرفر بحت، مفيش تغيير في التطبيق، مفيش بنية تحتية جديدة
- ابدأ **بالتعليم مش الرفض** — نفس منطق `log_only` اللي ماشي عليه
- بيمسك النسخ الحرفي، مش بيمسك مهاجم بيضيف ضوضاء عشوائية — وده مستوى مهارة تاني خالص

**(2) تفعيل Firebase App Check — مدفوع تمنه ومش مستخدم**

اكتشاف: `firebase_app_check: ^0.3.2` موجود في `pubspec.yaml`، لكن:

```bash
grep -rn "AppCheck\|app_check" lib/     # → لا شيء
grep -rln "AppCheck" backend_medjet/core backend_medjet/config   # → لا شيء
```

**الحزمة معطّلة تمامًا** — لا بتتفعّل في التطبيق ولا السيرفر بيتحقق منها. مذكورة في `CLAUDE.md` كأنها جزء من الحزمة وهي ميتة.

App Check ما بيمنعش العميل إنه يكذب — بيمنع **العميل المعدّل** إنه يتكلم أصلاً. وده بالظبط المطلوب هنا: APK معدّل بيفقد الشهادة.

- فعّلها في `initialization.dart` (Play Integrity على أندرويد، DeviceCheck على iOS)
- تحقق من الهيدر `X-Firebase-AppCheck` في `bootstrap.php` على مسارات الوجه أولًا
- **لازم تتدرّج:** البيلدات القديمة في المتجر مش بتبعتها. سجّل الغياب فترة، وما ترفضش غير لما تتأكد إن الإصدارات الجديدة انتشرت

**الحل المعماري النهائي (مش دلوقتي):** استخراج على السيرفر — العميل يبعت الصورة والسيرفر يستخرج. بيقفل الثغرة تمامًا **وبيحل مشكلة الويب في نفس الوقت** (نفس المستخرج للتطبيق والمتصفح). التكلفة: خدمة Python (PHP ما يشغّلش tflite)، ونطاق تردد، و**بيلغي قرار الخصوصية المتعمّد** إن الصورة ما تسبش الموبايل — وده له تبعات تحت 14/2025.

---

### ب-1ب ⚠️ تحديد المعدل بالـ IP بقى بلا معنى على قناة الويب

**الحالة:** مكتشَف أثناء اختبار المتصفح 2026-08-03. ✅ **اتصلح في نفس اليوم** —
طبقتان، ومن غير أي ثقة في هيدر:
- `limit_req zone=medjat_web` في nginx على العنوان الحقيقي — متحقَّق منه:
  54 من 120 طلب متوازي اترفضوا بـ503.
- `WebSessionService::enforcePerEmployeeLimit()` على `employee_id` — متحقَّق منه:
  60 نجحوا و15 اترفضوا بـ429.

كل طلبات المتصفح بتعدّي على بروكسي Next.js (`/api/employee/*`)، لأن بيانات الـ
Basic auth ما ينفعش تنزل للمتصفح. البروكسي بينادي الباك إند من السيرفر — يعني
`$_SERVER['REMOTE_ADDR']` بيبقى **IP خادم Next.js لكل الموظفين**.

النتيجة إن `RateLimiter::enforceIpLimit()` (600 طلب/دقيقة) اتحوّل من حد **لكل
عميل** لـ**ميزانية مشتركة** على القناة كلها:

- موظف واحد بتبعت صفحته طلبات كتير (زي الحلقة اللي اتصلحت فوق) **بيقفل القناة على
  الشركة كلها**
- ومهاجم واحد يقدر يعمل نفس الحاجة بقصد

**ملاحظة:** ده **مش** بيأثر على تطبيق الموبايل — بيكلّم الباك إند مباشرة، فالـ IP
بتاعه حقيقي. ولا على مسار Cloudflare — `conf.d/cloudflare-realip.conf` بيحطّ الـ
IP الحقيقي في `REMOTE_ADDR`. المشكلة في مسار البروكسي بس.

**اللي اتعمل — كل طبقة بتحدّ على اللي بتشوفه فعلاً:**

| الطبقة | بتشوف | الملف |
|---|---|---|
| nginx على `app.medjatapp.com` | العنوان الحقيقي (`cloudflare-realip.conf` بيحطه في `$binary_remote_addr`) | `device/nginx-web-zone.conf` — 20r/s وburst 40 |
| الباك إند بعد التوثيق | `employee_id` — ما يتزوّرش | `WebSessionService::enforcePerEmployeeLimit()` — 60/دقيقة |
| موجود من قبل | رقم التليفون على login/activate | حماية تخمين الـ PIN — **ما كانتش متأثرة أصلاً** |

**اللي اترفض ولِيه:** تمرير `X-Forwarded-For` والباك إند يثق فيه. بيخترع علاقة
ثقة جديدة، وإعداد غلط واحد بيحوّلها لتجاوز كامل — أي عميل يزوّر العنوان ويتهرب.
nginx بيقرا عنوان الاتصال نفسه، فمفيش حاجة تتزوّر.

⚠️ حد nginx **لكل عنوان**، ومكتب فيه 40 موظف ورا NAT بيبان عنوان واحد — علشان كده
الرقم سخي عن قصد. الشغل الدقيق بيعمله حد الموظف.

---

### ب-2 أعمدة الـ Kiosk اليتيمة على الإنتاج

**الحالة المتحقَّق منها:**

| | |
|---|---|
| `attendance_stations` / `station_recognition_logs` / `kiosk_pins` | ❌ مش موجودين |
| `branches.station_*` (6 أعمدة) | ✅ **لسه موجودين** |
| `attendance.station_id` / `recognition_confidence` / قيم `station_*` في `recognition_method` | ✅ موجودين |

ميجريشن `archive/2026_06_14_remove_kiosk_system.sql` بيعمل الاتنين، بس اللي اتطبّق فعليًا هو الجداول بس.

**الحل: سيبهم زي ما هما.** إنت قررت ترجع تبني الـ Kiosk، فالأعمدة دي **أصول مش مخلفات** — إعدادات الفرع وأعمدة تسجيل الحضور جاهزة ومطبّقة على الإنتاج بالفعل.

المطلوب حاجة واحدة: **وثّقها** عشان محدش يشيلها بحسن نية. أضف تعليق في `archive/2026_06_14_remove_kiosk_system.sql` بإن جزء منه اتطبّق وجزء لأ، وإن الأعمدة الباقية محجوزة لإعادة بناء الـ Kiosk.

⚠️ و`station_methods enum('face_only','fingerprint_only','both_available')` محتاجة مراجعة وقت البناء — البصمة محتاجة هاردوير التابلت مالوش.

---

### ب-3 التعرّف 1:N هيكسر العتبة في الـ Kiosk

**المشكلة:** في التطبيق الموظف داخل بحسابه → مقارنة **1:1**. في الـ Kiosk بيقف قدام الكاميرا من غير ما يقول هو مين → مقارنة بـ**كل** موظفين الفرع (1:N).

عند 0.45 القبول الخاطئ 0.2% للمقارنة الواحدة. فرع فيه 50 موظف = 50 مقارنة → **~10% احتمال تعرّف غلط لكل تسجيل**.

**الحل:** خلّي الموظف يقول هو مين الأول، والوش يأكّد — يفضل 1:1 والعتبة تفضل 0.45.

- **بادج QR** يقرّبه من كاميرا التابلت ← الأنسب للعمالة (مفيش حاجة تتحفظ)
- **PIN** كبديل لما الكارت يضيع

الكارت لوحده ما ينفعش (بيتعار) — والوش هو اللي بيمنع الإعارة. كل واحد فيهم بيسدّ ضعف التاني.

**ما تعملش:** وش لوحده (1:N) بعتبة عالية — بترجّعك لمشكلة الرفض الخاطئ اللي لسه مصلّحينها.

---

### ب-4 بورت 8090 مفتوح للعالم

**الحالة:** مفتوح، وإنت اخترت كده بعد ما قلتلك التكلفة.

**التكلفة الفعلية:** IP الأصل بقى مكشوف على البورت ده — Cloudflare ما بقاش بيخبّيه عن حد بيعمل scan.

**اللي بيحدّ منها (موجود فعلًا):** الـ vhost بيقدّم `/iclock/` بس وأي حاجة تانية 404 من غير ما تلمس PHP، وفيه `limit_req` 30r/s، والأجهزة المجهولة بتقع `unclaimed` بـ`tenant_id = NULL` و`DevicePunchIngestor` بيخزّن بصماتها **من غير** ما يقيّدها حضور.

**الحل لو غيّرت رأيك:** أول ما الفرع يبقى له IP ثابت، اقفلها عليه:

```bash
sudo ufw delete allow 8090/tcp
sudo ufw allow from <IP-الفرع> to any port 8090 proto tcp
```

**البديل الأنضف لو الأجهزة قليلة:** `import_punches.php` + `PunchCsvParser` شغّالين النهاردة و**مش محتاجين بورت مفتوح خالص**.

---

### ب-5 `medjat_central` لسه فيه 0.65 في يد المستخدمين

الكود اتعدّل لـ 0.45، بس ده تطبيق Flutter — محتاج build ورفع للمتجر.

**التقييم: مش مستعجل.** الشاشة بتقرا العتبة من الـ API (بترجّع 0.450 دلوقتي)، والقيمة المكتوبة بتشتغل بس لو الـ API ما ردّش. سيبها للإصدار الجاي.

---

### ب-6 شغل غير متعمول له commit

```
?? front_end/medjat_app/assets/models/mobilefacenet.tflite      (5.2 MB)
?? front_end/medjat_app/assets/models/mobilefacenet.LICENSE.txt
?? backend_medjet/migrations/2026_08_02_face_threshold_default.sql
?? attendance-methods-vs-competitors.md
?? specs/004-web-attendance-checkin/
 M backend_medjet/core/FaceMatchService.php
 M front_end/medjat_central/lib/.../attendance_method_controller.dart
 M front_end/medjat_app/assets/models/README.md
 M attendance-methods-implementable.md
```

**السيرفر شايل النسخة الصح، لكن git ما شافهاش.** أي `deploy.sh` من نسخة تانية هيرجّع الباك إند للقديم.

**الحل: اعمل commit.** والقرار الوحيد فيه: ملف الـ 5 ميجا يدخل git عادي ولا Git LFS.

---

## ج) قرارات مسجّلة — مش مشاكل

| البند | القرار |
|---|---|
| الويب ما يقدرش يعمل تحقق وش | قيد **دائم** مش مؤقت. المواصفة بتعالجه بالتقفيل الافتراضي + الصورة + كشف الجهاز المشترك |
| `ALLOWED` بتخلط الطريقة بالقيد | معروف ومؤجّل. `FR-023b` بيمنع ميزة الويب إنها تكبّر الخلط |
| `constitution.md` قالب فاضي | الخطة اتبوّبت على قواعد `CLAUDE.md` بدل اعتبارها نجاح تلقائي |
| WebAuthn | مرفوض لهذا الإصدار — محدش في السوق بيستخدمه للحضور |

---

## د) اتفحصت وطلعت سليمة ✅

**أرقام التليفون (E.164)** — كانت مسجّلة كخطر على أي قناة بتطابق بالرقم (واتساب/IVR/SMS).

```sql
SELECT COUNT(*) FROM employees WHERE phone IS NOT NULL AND phone<>'';        -- 10
SELECT COUNT(*) FROM employees WHERE phone REGEXP '^\\+[0-9]{1,3}0';          -- 0
SELECT COUNT(*) FROM employees WHERE phone NOT LIKE '+%' AND phone<>'';       -- 0
```

**صفر صف فيه صفر محلي زيادة، وصفر صف من غير `+`.** الخطر ده مش موجود على القاعدة الحالية. لو اتعمل استيراد كبير في المستقبل، أعد الاستعلام قبل بناء أي قناة بتطابق بالرقم.

---

## الترتيب المقترح

1. **ب-6 (commit)** — دقايق، وبيحمي كل شغل اليومين دول من إنه يتمسح بـ deploy
2. **ب-1 طبقة (1)** كشف التكرار — شغل سيرفر بحت، بيمسك الثغرة الحقيقية الوحيدة في كود منشور
3. **ب-1 طبقة (2)** App Check — تفعيل حاجة موجودة ومعطّلة، بالتدرّج
4. **ب-2** تعليق توثيقي على ميجريشن الأرشيف — دقيقتين، وبيمنع إن حد يشيل أعمدة إنت محتاجها
5. الباقي مع الشغل اللي بيخصه
