# Next.js app (medjat_central_web) — app.medjatapp.com
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name app.medjatapp.com;
    ssl_certificate     /etc/ssl/certs/medjat-wildcard-origin.pem;
    ssl_certificate_key /etc/ssl/private/medjat-wildcard-origin.key;
    client_max_body_size 25m;

    location / {
        # Coarse flood protection on the real client address (see
        # conf.d/medjat-web-zone.conf for why this lives here and not in PHP).
        limit_req zone=medjat_web burst=40 nodelay;

        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_read_timeout 120s;
    }
}

# Promo / landing site — medjatapp.com + www
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name medjatapp.com www.medjatapp.com;
    ssl_certificate     /etc/ssl/certs/medjat-wildcard-origin.pem;
    ssl_certificate_key /etc/ssl/private/medjat-wildcard-origin.key;

    root /var/www/medjat-web/promo;
    index index.html;

    # App/Universal-link association files (served from backend copies)
    location = /.well-known/apple-app-site-association {
        default_type application/json;
        alias /var/www/medjat/backend/.well-known/apple-app-site-association;
    }
    location = /.well-known/assetlinks.json {
        default_type application/json;
        alias /var/www/medjat/backend/.well-known/assetlinks.json;
    }

    # Employee join fallback page (PHP) + its stylesheet
    location = /join {
        fastcgi_pass unix:/run/php/php8.5-fpm.sock;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/medjat/backend/join.php;
        fastcgi_param SCRIPT_NAME /join;
    }
    location = /public/join.css {
        alias /var/www/medjat/backend/public/join.css;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
