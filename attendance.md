# الحضور والانصراف في مجات — المرجع الموحّد

> **ملف واحد بديل لأربع ملفات** اتدمجوا هنا بتاريخ **2026-08-06**:
> `attendance-methods-implementable.md` (خريطة التنفيذ، 07-24 → 07-31) ·
> `attendance-methods-proposed.md` (الفجوات المقترحة، 07-31) ·
> `attendance-methods-vs-competitors.md` (المقارنة التنافسية، 08-02) ·
> `attendance-open-issues.md` (المشاكل المفتوحة، 08-03).
> خلاصة مسح السوق (`compass_artifact_*.md`) في القسم 8، والملف الأصلي متساب مكانه كمرجع خام.
>
> **المنهج:** كل حقيقة عن مجات **متحقَّق منها من الكود** وقت الدمج. كل حقيقة عن منافس من
> **مواد تسويقية** — اقراها على إنها "بيعلنوا إنهم بيعملوا كده"، مش قياس مستقل.
>
> ⚠️ **الملفات المدموجة كانت قديمة.** اتنفّذ حاجتين كبار بعد آخر تحديث ليها:
> **التسجيل من المتصفح** (`0708ce1`) و**كيوسك الفرع** (`fd8747e`). القسمين 1 و2 اتحدّثوا،
> والفجوات اللي اتقفلت اتشالت من قائمة الفجوات ودخلت "الوضع الحالي".

---

## 1) الوضع الحالي — اللي شغّال فعلاً

`AttendanceMethodResolver::ALLOWED` (`core/AttendanceMethodResolver.php:27`):

```php
['qr_gps', 'gps_only', 'face_selfie', 'wifi_gps', 'device', 'manual', 'kiosk']
```

### 1.1 الطرق

| الطريقة | التفاصيل الفعلية | نقطة الدخول |
|---|---|---|
| `qr_gps` | QR ملك الفرع (`branches.qr_code`)، الموظف يمسحه + geofence | `app/attendance/check_in.php` → `GpsService::validateCheckIn` |
| `gps_only` | geofence لكل فرع بنصف قطر (`GpsService`) | نفس المسار بدون QR |
| `wifi_gps` | **BSSID أو IP أو CIDR** — `branch_networks.kind`، ووضع تعلّم بيكتشف نقاط الوصول تلقائيًا. **قيد فوق الـ geofence مش بديل عنه** | نفس المسار + `NetworkVerifier::verify` |
| `face_selfie` | الموبايل يستخرج الـ embedding، **السيرفر** يطابق، nonce أحادي الاستخدام | نفس المسار + `FaceMatchService::verify` |
| `device` | ZKTeco ADMS push مباشر + استيراد CSV | `device/iclock.php` → `DevicePunchIngestor` |
| `kiosk` | تابلت مشترك على باب الفرع — تعرّف 1:N بقاعدة هامش + PIN + كود دخول | `app/kiosk/*` |
| `manual` | إدخال المدير (لصاحب صلاحية) | `app/attendance/manual_check_in.php` |

**التسجيل الذاتي من تطبيق الموظف** صالح لـ `qr_gps` / `gps_only` / `face_selfie` / `wifi_gps` بس
(`check_in.php:47`). `manual` بيتسجّل من الإدارة، `device` جاي من ترمينال، و`kiosk` من التابلت.

### 1.2 القنوات — وده **مش** نفس مفهوم الطريقة

الفرق ده مهم ومتنفّذ صح في الكود: **الويب قناة مش طريقة.**

```php
// check_in.php:21-25 — القناة جاية من الجلسة الموثّقة، مش من الطلب أبدًا،
// عشان متصفح ما يقدرش يتنكّر في صورة تسجيل من التطبيق.
$isWeb  = ($auth['platform'] ?? null) === 'web';
$origin = $isWeb ? 'web' : 'app';
```

| القناة | مين بيوثّق | ملاحظات |
|---|---|---|
| تطبيق الموظف | `X-Employee-Token` | الـ IP حقيقي، الطابور offline (Hive) شغّال |
| المتصفح | جلسة ويب عبر بروكسي Next.js | `WebAccessPolicy` + `WebSessionService`؛ **مقفول افتراضيًا** لكل شركة |
| الكيوسك | `X-Kiosk-Token` — **مبدأ توثيق تالت، بيوثّق كـ«فرع» مش كشخص** | `Auth::authenticateKiosk()` بيرجّع **بدون** `employee_id` عن قصد |
| الترمينال | السيريال + ADMS | جهاز مجهول بيقع `unclaimed` بـ `tenant_id = NULL` |

**قناة الويب** بتفرض صورة لو الشركة مفعّلاها (`WebAccessPolicy::photoRequired` →
`PunchPhotoService::store`)، وبترفض بـ `PHOTO_REQUIRED` لو الصورة ناقصة — فمستحيل يتسجّل حضور
من غير صورة في الشركة اللي طالباها.

### 1.3 البنية اللي بتخدم أي طريقة جديدة

- **تجاوز الطريقة على 4 مستويات:** موظف ← فئة (union) ← فرع ← شركة.
- **التوقيت لكل شركة** عبر `core/TenantClock.php` — مش `date()` ولا `NOW()` مباشرة.
- **كشف التحايل:** `is_vpn` / `is_mock_location` / `is_rooted_device` + جدول
  `attendance_security_logs` (اتعمل على القاعدة الحيّة 2026-07-31). رفض الـ mock location
  **اختياري لكل شركة و على Android بس**؛ الـ root **متعمّد إنه ما يتحجبش** (شائع على أجهزة
  رخيصة ومش دليل غش).
- **Geofence** لكل فرع بنصف قطر (`GpsService`, `DEFAULT_GPS_RADIUS`).
- **عتبة الوجه** = `FaceMatchService::DEFAULT_THRESHOLD = 0.450` (اتصلّحت من 0.650 اللي كانت
  بترفض ~52% من المحاولات الصحيحة).

### 1.4 الكيوسك — النقطة الوحيدة اللي مالهاش نظير في باقي الكود

التطبيق بيسأل **«هو ده أحمد؟»** (1:1). الكيوسك بيسأل **«ده مين؟»** (1:N) — ودي مش نفس المشكلة
بحلقة حواليها. القبول الخاطئ بيتراكم: عند معدل *p* لكل مقارنة، مسح *N* مرشّح =~ `1 − (1−p)^N`.

مقاس على 800 زوج من LFW:

| العتبة | FAR لكل مقارنة | FRR | FAR عبر 40 | عبر 200 |
|---|---|---|---|---|
| 0.30 | 4.2% | 6.5% | 82% | ~100% |
| 0.40 | 0.8% | 16% | 27% | 80% |
| 0.45 | 0.2% | 19% | 7.7% | **33%** |

يعني عند العتبة اللي المسار 1:1 شغّال بيها، فرع بـ200 موظف = **احتمال واحد من تلاتة** إن
البصمة تتنسب للشخص الغلط. رفع العتبة لوحده بيبدّل ده بمعدل رفض محدش هيستحمله.

**اللي بيخلّيه آمن: قاعدة الهامش** — المرشّح الأعلى لازم **يعدّي العتبة** *و* يبعد بهامش كافٍ
عن تاني أقرب مرشّح. وفوقها **كود دخول / PIN** يقول الموظف مين الأول فيرجع المسار 1:1
(`identify_by_code.php`, `set_pin.php`, `station_code_fallback_enabled`).

الميجريشنز: `2026_08_03_kiosk_1..5` + `2026_08_04_kiosk_6..8`.

---

## 2) المقارنة بالمنافسين

✅ موجود · ⚠️ جزئي · ❌ مش موجود · **🆕 = اتقفلت من وقت مقارنة 08-02**

| الطريقة | **مجات** | Jibble | Connecteam | Deputy | Buddy Punch | ZenHR | Nova HR | Bayzat |
|---|---|---|---|---|---|---|---|---|
| موبايل + GPS/geofence | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| سيلفي + تعرّف وجه | ✅ | ✅ | ⚠️ سيلفي | ✅ | ✅ | ❌ | ✅ | ✅ |
| QR | ✅ | ⚠️ | ✅ | ⚠️ | ✅ | ✅ | ⚠️ | ⚠️ |
| تحقق WiFi BSSID | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| تحقق IP | ✅ | ❌ | ❌ | ⚠️ | ⚠️ | ❌ | ❌ | ✅ |
| أجهزة بصمة (ZKTeco) | ✅ مباشر | ❌ | ❌ | ❌ | ❌ | ⚠️ عبر BioTimeCloud | ✅ | ✅ عبر شركاء |
| إدخال يدوي للمدير | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| تابلت مشترك (Kiosk) | **🆕 ✅** | ✅ | ✅ | ✅ | ✅ | ⚠️ | ❌ | ❌ |
| PIN | **🆕 ✅** | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| تسجيل من المتصفح | **🆕 ✅** | ✅ | ⚠️ | ✅ | ✅ | ✅ | ❌ | ⚠️ |
| صورة بدون مطابقة وش | **🆕 ⚠️ للويب بس** | ⚠️ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| طابور offline | ✅ | ✅ | ✅ | ✅ | ⚠️ | ❌ | ❌ | ❌ |
| RFID | ⚠️ عبر الترمينال | ✅ | ⚠️ | ❌ | ❌ | ⚠️ | ⚠️ | ⚠️ |
| **NFC** | ❌ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **دخول/خروج تلقائي بالـ geofence** | ❌ | ⚠️ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **تتبّع المسار أثناء الوردية** | ❌ | ⚠️ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **الربط بمشروع/أمر شغل** | ❌ | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |

> Workyard وHubstaff وbusybusy (متخصصين إنشاءات) مش منافسين مباشرين، بس مرجع لبندين:
> **المسار أثناء الوردية** و**الربط بمشروع**.

### 2.1 اللي مجات عندها والمنافسين لأ (الاتجاه العكسي)

1. **تحقق WiFi BSSID مع وضع تعلّم.** المنافسين المحليين (EdaraHR، Flowdo) وBayzat بيعلنوا
   **IP restriction** بس. مجات عندها BSSID **و** IP **و** CIDR، باكتشاف تلقائي لنقاط الوصول
   ونسبة تغطية بتحكم التفعيل.
2. **الوجه بمطابقة على السيرفر مع nonce أحادي الاستخدام** — قرار معماري متعمّد ضد الـ APK
   المعدّل (وشوف حدوده في 5-ب1).
3. **ZKTeco مباشر بـ ADMS** — ZenHR بتمر عبر BioTimeCloud (وسيط)، Bayzat عبر شركاء.
4. **تجاوز الطريقة على 4 مستويات** مع union على الفئات — الحبيبات دي غير معلَنة عند حد.
5. **طابور offline** في تطبيق الموظف (Hive + مزامنة).
6. **مصري أصلاً:** عربي RTL أول، قانون 14/2025، توقيت لكل شركة **بالاسم مش بأوفست**.

---

## 3) الفجوات المتبقية — القائمة الموحّدة

اتدمجت هنا فجوات المنافسين + المقترحات، واتشال كل اللي اتقفل. الترتيب حسب **العائد لكل ساعة شغل**.

### 3.1 الأولى للتنفيذ

| # | البند | الحالة | الشريحة اللي بتفتحها |
|---|---|---|---|
| 1 | QR متجدد زمنيًا | ✅ **اتبنى 2026-08-06** — مستنّي `deploy.sh` | كل مستخدمي `qr_gps` — سد ثغرة |
| 2 | قفل الجهاز + بيومترية الموبايل | ✅ **مقفول أصلاً** — تفصيل تحت | الكل |
| 3 | `photo_gps` كطريقة عامة | ✅ **اتبنى 2026-08-06** — مستنّي `deploy.sh` | الشركات المتحفظة على البيومتري |
| 4 | الربط بمشروع/أمر شغل | ⏸️ محتاج مواصفة | الإنشاءات والمقاولات |
| 5 | `visit_gps` — geofence لكل عميل | ⏸️ محتاج مواصفة | المندوبين (FMCG وأدوية) |

> **البند 2 اتشطب بعد مراجعة الكود، مش بعد تنفيذه.** طلع مقفول بتلات طبقات موجودة:
> بصمة الموبايل متنفّذة بالكامل (`local_auth` في التطبيق + `require_local_biometric`
> بيتفرض في `check_in.php` و`check_out.php`)، وكل تسجيل دخول محتاج **كود تفعيل أحادي
> الاستخدام** من الإدارة (`ActivationCodeModel`) يعني الجهاز الجديد محتاج موافقة أصلاً،
> و`EmployeeAuthTokenModel::issue()` بيلغي توكن الموبايل السابق. `employees.active_device_id`
> كان هيبقى طبقة رابعة على نفس الثغرة المقفولة.

**1) QR متجدد زمنيًا بدل الثابت** — الـ QR المطبوع في الفرع أكبر ثغرة عملية: حد يصوّره ويبعته
على واتساب. الحل شاشة بتعرض QR بيتغيّر كل 30 ثانية بنفس منطق الـ nonce الشغّال في
`face_challenges` (`nonce` + `expires_at` + `consumed_at` + فهرس فريد). المطلوب: جدول
`qr_challenges` (أو `purpose` جديد في الموجود) + endpoint توليد + تحقق في `check_in.php` +
صفحة عرض في `medjat_central_web`. **حساب الانتهاء في SQL** (`DATE_ADD(NOW(), INTERVAL ? SECOND)`).
أعلى عائد لكل ساعة شغل في الملف كله.

**2) قفل الجهاز (device binding) + `local_auth`** — الموظف يدّي موبايله لزميله؛ ده الـ
buddy punching الحقيقي في السوق. النمط **متنفّذ بالفعل للأدمن**: `admins.active_device_id` +
هيدر `X-Device-Id` (جهاز واحد، الأحدث يكسب)، و`employee_auth_tokens.device_id` موجود بس
**بدون قفل**. المطلوب: `employees.active_device_id` + نفس المنطق + `local_auth` في التطبيق
(مش في `pubspec.yaml` لسه) + مسار "طلب تغيير الجهاز" (الموبايل بيضيع وبيتباع، لازم مخرج).
بيقفل الثغرة **من غير ML ومن غير موافقة بيومترية مطلوبة قانونًا**.

**3) `photo_gps` كطريقة عامة** — دلوقتي الصورة-بدون-مطابقة موجودة **في قناة الويب بس**
(`WebAccessPolicy::photoRequired`). خلّيها طريقة كاملة متاحة للتطبيق كمان: صورة + GPS + وقت
تتخزن **كدليل بس**، من غير embedding ولا قرار آلي. مسار الرفع موجود من `face_selfie` —
المطلوب إنك **ما تشغّلش** `FaceMatchService`. بتدّي ~80% من الردع بصفر التزام بيومتري تحت 14/2025.

**4) الربط بمشروع/أمر شغل (Job Costing)** — الحضور مربوط بـ**فرع** بس. ده **مش طريقة إثبات
هوية، ده بُعد ناقص في البيانات**: بيجاوب على "بيشتغل على إيه" مش "مين وفين". وده السبب اللي
بيخلّي شركة مقاولات تدفع في نظام حضور أصلاً. بيغيّر جدول `attendance` والتقارير والـ payroll.

**5) `visit_gps` — geofence لكل موقع مش لكل فرع** — الـ geofence مربوط بـ`branches`
(`latitude`/`longitude`/`gps_radius_meters`). مندوب بيزور 15 عميل في اليوم **مالوش فرع**، يعني
سوق المندوبين كله شغّال على `gps_only` بلا تحقق حقيقي. المطلوب: جدول `sites`/`clients` + خط
سير + تعميم `GpsService` + إعادة تفكير في شكل يوم العمل (زيارات متعددة بدل check-in/out واحد).
**أكبر تمايز تجاري متبقّي** — سوق FMCG والأدوية في مصر ضخم ومحدش من المنافسين المحليين مغطّيه.

### 3.2 قنوات جديدة (بتفتح شرايح مقفولة)

**واتساب (`whatsapp_gps`)** — الموظف يبعت Location Pin على رقم الشركة → webhook → نفس تحقق
`GpsService`. واتساب هو الواجهة الافتراضية في مصر: مفيش تطبيق يتنزّل ولا تدريب. المطلوب:
WhatsApp Cloud API + webhook + ربط الرقم بالموظف.
⚠️ **مخاطرة:** الـ Location Pin بيتزوّر بنفس تطبيقات Fake GPS و**مفيش عندك إشارة
`is_mock_location`** في المسار ده — الحماية أضعف من التطبيق. قناة للشركات اللي الثقة فيها
مقبولة، مش للمواقع الحسّاسة. **الجهد:** متوسط.

**ميس كول / IVR / SMS** — شريحة كبيرة في مصر (أمن، نظافة، عمالة يومية) مالهاش سمارت فون.
الكيوسك قفل جزء كبير من الفجوة دي، **بس الكيوسك محتاج تابلت في مكان ثابت** — العامل اللي
بيتوزّع على مواقع لسه مقفول عليه. الميس كول من رقمه المسجّل → caller ID → بصمة زمنية.
**قيود صريحة:** مفيش موقع، والموبايل يتعار — **أضعف طريقة أمنيًا**، لكنها أحسن من الورق.
تتقيّد بنافذة زمنية ضيقة حوالين الوردية + مراجعة المشرف. **الجهد:** صغير–متوسط (التكلفة في
البوابة مش في الكود).

**تسجيل الطاقم بواسطة المشرف (`crew_gps`)** — المشرف في الموقع يعلّم الحاضرين، ويتسجّل
**GPS واحد + صورة جماعية + وقت** للمجموعة. الفرق عن `manual`: ده ميداني بإحداثيات وصورة
ومسؤول محدد بالاسم (`recorded_by_employee_id`). سوق الإنشاءات والزراعة في مصر شغّال كده فعليًا.
**الجهد:** متوسط.

### 3.3 إضافات تكتيكية (حسب طلب العملاء)

| البند | التقييم |
|---|---|
| **حضور افتراضي بالجدول** (`scheduled`) | الموظف المكتبي/الريموت يتحسب حاضر حسب ورديته إلا لو اتسجّل استثناء. الجداول موجودة، وكرون الغياب (23:50) بيعمل نص الشغل — المطلوب قلب المنطق + واجهة استثناءات. صغير–متوسط |
| **دخول/خروج تلقائي بالـ geofence** | قيمة `'auto'` موجودة في enum `check_out_method` بس **محدش بيكتبها**. متوسط |
| **تتبّع المسار أثناء الوردية** | ⛔ **متعملهاش دلوقتي** — حسّاسة قانونيًا تحت 14/2025 ومنقولة من سوق أمريكي. المنافسين اللي بيعملوها بيبيعوا لمقاولين أمريكان |
| **NFC** | بديل للـ QR بنفس مستوى الأمان تقريبًا. **مش أولوية** طالما `qr_gps` شغّالة — إلا لو عميل طلبها بالاسم |
| **Bluetooth Beacon** | بديل داخلي للـ GPS الضعيف جوّه المباني. من الصفر، متوسط |
| **RFID / Barcode badges** | بقى ممكن عبر ترمينالات ZKTeco الشغّالة. متوسط |
| **Hikvision** | ZKTeco اتعمل؛ Hikvision نفس الشغل تاني |
| **تسجيل دخول الكمبيوتر / إضافة متصفح / Slack-Teams** | للـ BPO وشركات البرمجة (مصر مركز BPO كبير). قناة الويب غطّت الأساس منها |

---

## 4) الدين المعماري — اقرا ده قبل إضافة أي طريقة ⚠️

`ALLOWED` **بتخلط الطريقة بالقيد**، و`check_in.php` شجرة `if` على الأسماء:

```php
ALLOWED = ['qr_gps','gps_only','face_selfie','wifi_gps','device','manual','kiosk'];

if (!in_array($requestedMethod, ['qr_gps','gps_only','face_selfie','wifi_gps'], true)) { ... }
if ($requestedMethod === 'wifi_gps')    { /* NetworkVerifier */ }
if ($requestedMethod === 'face_selfie') { /* FaceMatchService */ }
```

`qr_gps` و`wifi_gps` و`face_selfie` كلهم = **GPS + حاجة زيادة**. لو ضفت الطرق اللي فوق، عدد
التركيبات بينفجر: `photo_wifi_gps`؟ `nfc_face_gps`؟ `qr_rotating_wifi_gps`؟ وكل تركيبة =
عنصر في `ALLOWED` + فرع في `check_in.php`/`check_out.php` + خيار في **4 واجهات اختيار**.

**البديل — فصل المفهومين:**

| المفهوم | الأمثلة |
|---|---|
| **طريقة** (إثبات إن ده أنا) | `self` / `face` / `photo` / `whatsapp` / `phone_call` / `device` / `kiosk` / `crew` / `manual` |
| **قيود** (شروط لازم تتحقق) | `gps` / `qr` / `wifi` / `rotating_qr` / `bound_device` / `local_biometric` / `schedule_window` |

الـ resolver يرجّع `{method, constraints[]}`، و`check_in.php` يلف على القيود بدل شجرة `if`.
القيود تتخزن JSON على نفس مستويات التجاوز (موظف ← فئة ← فرع ← شركة).

**السابقة اللي تبني عليها:** فصل **القناة** (`$origin = web|app`) عن الطريقة اتعمل صح بالفعل
في ميزة الويب، والقناة بتيجي من الجلسة الموثّقة مش من الطلب. نفس المبدأ بالظبط ينطبق على القيود.

**التوقيت:** الريفاكتور ده كان يستاهل **قبل** الكيوسك — دلوقتي `ALLOWED` بقت 7 عناصر وفيها
`kiosk` اللي هو أصلاً **قناة** مش طريقة. اعمله قبل `photo_gps` و`visit_gps`، مش بعدهم. ولما
تعمله، خلّي القيم القديمة (`qr_gps` إلخ) **تتقرأ وتتحوّل تلقائيًا** — فيه تطبيقات في الستور
لسه بتبعت الأسماء دي (`check_in.php` فيه بالفعل fallback للبيلدات القديمة اللي مش بتبعت `method`).

---

## 5) المشاكل المفتوحة

### أ) اتقفلت ✅

| # | المشكلة | الحل اللي اتنفّذ |
|---|---|---|
| أ-1 | `mobilefacenet.tflite` مش في المستودع | اتضاف (BSD-3 + إشعار الرخصة). الجراف متحقَّق `[1,112,112,3]`→`[1,192]` |
| أ-2 | العتبة 0.650 بترفض 52% من المحاولات الصحيحة | **0.450** — الثابت + `DEFAULT` العمود + الست شركات، منشور ومتحقَّق |
| أ-3 | ZKTeco مبني ومش منشور | `nginx-devices.conf` + `limit_req_zone` + `ufw allow 8090/tcp`؛ متحقَّق من بره |
| ب-1ب | تحديد المعدل بالـ IP بلا معنى على قناة الويب | طبقتين بدون ثقة في أي هيدر — تفاصيل تحت |
| ب-2 | أعمدة الكيوسك اليتيمة | **اتحلّت بإعادة بناء الكيوسك** — الأعمدة بقت مستخدَمة فعلاً |
| ب-3 | التعرّف 1:N هيكسر العتبة | قاعدة الهامش + كود دخول/PIN يرجّع المسار 1:1 |
| ب-5 | `medjat_central` فيه 0.65 مكتوبة | اتشالت — مفيش `0.65` في `medjat_central/lib` |
| ب-6 | شغل غير متعمول له commit | اتعمل — `4e71738`, `0708ce1`, `b722ddd`, `fd8747e` |
| د | أرقام التليفون E.164 | اتفحصت على القاعدة الحيّة: **صفر صف** فيه صفر محلي زيادة، **صفر صف** من غير `+` |

**تفصيل ب-1ب** (اتصلح 2026-08-03): كل طلبات المتصفح بتعدّي على بروكسي Next.js
(`/api/employee/*`)، فـ`REMOTE_ADDR` بيبقى **IP خادم Next.js لكل الموظفين** — يعني
`RateLimiter::enforceIpLimit()` اتحوّل من حد لكل عميل لـ**ميزانية مشتركة** على القناة كلها.
الحل: كل طبقة بتحدّ على اللي بتشوفه فعلاً —

| الطبقة | بتشوف | الملف |
|---|---|---|
| nginx على `app.medjatapp.com` | العنوان الحقيقي (`cloudflare-realip.conf`) | `device/nginx-web-zone.conf` — 20r/s burst 40 |
| الباك إند بعد التوثيق | `employee_id` — ما يتزوّرش | `WebSessionService::enforcePerEmployeeLimit()` — 60/دقيقة |

**اللي اترفض ولِيه:** تمرير `X-Forwarded-For` والباك إند يثق فيه — بيخترع علاقة ثقة جديدة،
وإعداد غلط واحد بيحوّلها لتجاوز كامل. nginx بيقرا عنوان الاتصال نفسه فمفيش حاجة تتزوّر.
⚠️ حد nginx **لكل عنوان**، ومكتب فيه 40 موظف ورا NAT بيبان عنوان واحد — علشان كده الرقم سخي
عن قصد، والشغل الدقيق بيعمله حد الموظف. (**مش** بيأثر على تطبيق الموبايل — بيكلّم الباك إند
مباشرة فالـ IP بتاعه حقيقي.)

---

### ب-1 ⚠️ الثغرة الحقيقية الوحيدة المتبقية: السيرفر بيصدّق العميل في الوجه

**الحالة: لسه مفتوحة — متحقَّق منها في كود 2026-08-06.**

`core/FaceMatchService.php` بياخد من العميل:

```php
$livenessPassed = !empty($input['liveness_passed']);                    // :215 — بوليان من العميل
$candidate = self::parseEmbedding($input['face_embedding'] ?? null);    // :228 — 192 رقم من العميل
```

**السيرفر ما بيشوفش الصورة أبدًا.** فبيلد معدّل يقدر:

```
يطلب nonce جديد → يبعت الـ embedding بتاعه (اتصوّر مرة واحدة قانونيًا واتخزّن)
                → يبعت liveness_passed: true → مقبول، والكاميرا ما فتحتش
```

الـ nonce بيمنع إعادة نفس **الطلب**، مش إعادة استخدام نفس **الأرقام** مع nonce جديد.
فمقولة *"لا تصدّق حكم العميل"* صح في نص واحد بس: السيرفر بيحسب التشابه بنفسه ✅ — لكنه
**بيصدّق** إن الأرقام دي جات من صورة اتاخدت دلوقتي، و**بيصدّق** إن كشف الحياة عدّى.

> نفس الملاحظة بتنطبق على `app/kiosk/identify.php` — بياخد `embedding` و`liveness_passed`
> من الجهاز بنفس الطريقة. الفرق إن الكيوسك جهاز الشركة نفسها مش موبايل الموظف، فالحافز أقل.

#### الحل — طبقتين، الاتنين رخاص

**(1) كشف التكرار على السيرفر — الأرخص وبيمسك الهجوم ده بالذات.** بصمة الوش الحقيقية
**ما بتتكررش حرفيًا**: كاميرا + إضاءة + ميل الرأس بيدّوا أرقام مختلفة كل مرة. فـembedding
**مطابق تمامًا** لمحاولة سابقة = مستحيل من كاميرا.

```
خزّن hash آخر N محاولة لكل موظف في face_verification_logs
لو التشابه مع محاولة سابقة لنفس الموظف > 0.9995
   → علّمها replayed_embedding في attendance_security_logs
```

شغل سيرفر بحت، مفيش تغيير في التطبيق. **ابدأ بالتعليم مش الرفض** — نفس منطق `log_only`.
بيمسك النسخ الحرفي، مش بيمسك مهاجم بيضيف ضوضاء عشوائية (مستوى مهارة تاني خالص).

**(2) تفعيل Firebase App Check — مدفوع تمنه ومش مستخدم.** `firebase_app_check: ^0.3.2`
في `pubspec.yaml`، لكن `grep -rn "AppCheck\|app_check"` على `medjat_app/lib` و`core/`
و`config/` بيرجّع **لا شيء** (متحقَّق 2026-08-06 — لسه معطّلة). App Check ما بيمنعش العميل
إنه يكذب، بيمنع **العميل المعدّل** إنه يتكلم أصلاً — وده بالظبط المطلوب. فعّلها في
`initialization.dart` (Play Integrity / DeviceCheck) وتحقق من `X-Firebase-AppCheck` في
`bootstrap.php` على مسارات الوجه أولًا. **لازم تتدرّج:** البيلدات القديمة في المتجر مش
بتبعتها — سجّل الغياب فترة قبل الرفض.

**الحل المعماري النهائي (مش دلوقتي):** الاستخراج على السيرفر — العميل يبعت الصورة والسيرفر
يستخرج. بيقفل الثغرة تمامًا **وبيحل مشكلة الويب في نفس الوقت** (نفس المستخرج للتطبيق
والمتصفح والكيوسك). التكلفة: خدمة Python (PHP ما يشغّلش tflite) + نطاق تردد + **إلغاء قرار
الخصوصية المتعمّد** إن الصورة ما تسبش الجهاز — وده له تبعات تحت 14/2025.

---

### ب-7 ✅ اتصلحت: إنيوم سجل الأمان فقد ثلات قيم في تصادم ميجريشن

**اتكشفت واتصلحت 2026-08-06** أثناء مراجعة الكود قبل بناء `photo_gps`.

ميجريشنان بنفس التاريخ الاتنين بيعملوا `MODIFY` لعمود `attendance_security_logs.reason`،
وكل واحد أعاد سرد الإنيوم كاملًا من نقطة بدايته هو:

| الملف | ضاف | الترتيب |
|---|---|---|
| `2026_08_03_attendance_punch_photo.sql` | `web_not_permitted`, `web_pin_locked`, `web_shared_device` | اتنفّذ **الأول** (`a` قبل `k`) |
| `2026_08_03_kiosk_4_enum_widening.sql` | ست قيم `kiosk_*` | اتنفّذ **التاني** — وكان مكتوب على الإنيوم *قبل* ملف الويب |

`MODIFY` بيستبدل تعريف الإنيوم بالكامل مش بيضيف عليه، فملف الكيوسك **شال القيم التلاتة
بتاعة الويب**. متحقَّق على الإنتاج 2026-08-06.

**وليه محدش لاحظ:** تلات مسارات منشورة بتكتب القيم دي (`WebAccessPolicy` ←
`web_not_permitted`، `SharedDeviceDetector` ← `web_shared_device`،
`employee_web_login.php` ← `web_pin_locked`)، وكلهم بيعدّوا على
`AttendanceSecurityModel::log()` اللي حاطط الـ`INSERT` جوّه `try/catch` وبينادي
`error_log()` بس. والإنتاج شغّال بـ`STRICT_TRANS_TABLES` (متحقَّق) فالقيمة برّه المدى
**بترمي خطأ** (`ERROR 1265`) — يعني كل سجل من دول اترمى بصمت.

دي **تاني مرة** الجدول ده يرمي رفضات بصمت (الأولى 2026-07-31 لما الجدول ما كانش موجود
على الإنتاج أصلاً). نفس نمط الفشل: سجل أمني بيفشل بهدوء ما ينفعش تفرّقه عن سجل أمني
مالوش حاجة يقولها.

**الإصلاح:** `2026_08_06_restore_web_security_log_reasons.sql` بيعيد سرد الـ17 قيمة كلها.

⚠️ **قاعدة للمرة الجاية:** قبل ما تضيف قيمة لإنيوم، اقرا التعريف **الحالي من قاعدة
البيانات** مش من آخر ميجريشن لمسه. ميزتين شغّالين في نفس الوقت كفاية للتصادم.

---

### ب-4 بورت 8090 مفتوح للعالم

**الحالة:** مفتوح، وده اختيار متاخد بعد معرفة التكلفة.

**التكلفة الفعلية:** IP الأصل مكشوف على البورت ده — Cloudflare مش بيخبّيه عن حد بيعمل scan.

**اللي بيحدّ منها (موجود فعلًا):** الـ vhost بيقدّم `/iclock/` بس وأي حاجة تانية 404 من غير ما
تلمس PHP، وفيه `limit_req` 30r/s، والأجهزة المجهولة بتقع `unclaimed` بـ`tenant_id = NULL`
و`DevicePunchIngestor` بيخزّن بصماتها **من غير** ما يقيّدها حضور.

**لو غيّرت رأيك** — أول ما الفرع يبقى له IP ثابت:

```bash
sudo ufw delete allow 8090/tcp
sudo ufw allow from <IP-الفرع> to any port 8090 proto tcp
```

**البديل الأنضف لو الأجهزة قليلة:** `app/devices/import_punches.php` + `core/PunchCsvParser.php` شغّالين النهاردة
و**مش محتاجين بورت مفتوح خالص**.

---

### ج) قرارات مسجّلة — مش مشاكل

| البند | القرار |
|---|---|
| الويب ما يقدرش يعمل تحقق وش | قيد **دائم** مش مؤقت. المواصفة بتعالجه بالتقفيل الافتراضي + الصورة + كشف الجهاز المشترك |
| `ALLOWED` بتخلط الطريقة بالقيد | معروف ومؤجّل — `FR-023b` منع ميزة الويب إنها تكبّر الخلط. القسم 4 |
| WebAuthn | مرفوض لهذا الإصدار — محدش في السوق بيستخدمه للحضور |
| `station_methods enum('face_only','fingerprint_only','both_available')` | محتاجة مراجعة — البصمة محتاجة هاردوير التابلت مالوش |
| الروت (root) ما يتحجبش | متعمّد — شائع على أجهزة رخيصة ومش دليل غش |

---

## 6) قواعد لازمة قبل أي طريقة جديدة

1. **التوقيت:** أي كتابة وقت تعدّي على `core/TenantClock.php`، مش `date()` ولا `NOW()`.
   وحسابات الانتهاء/الصلاحية **في SQL** مش في PHP — PHP بيشتغل UTC والـMySQL على توقيت
   السيرفر، فالكود اللي بيتحسب في PHP بيتولد منتهي. استخدم **اسم** المنطقة مش أوفست ثابت
   (مصر عندها توقيت صيفي، الخليج لأ).
2. **الأمان:** أي إشارة جاية من العميل (موقع، "أنا اتحققت") **مش مصدر ثقة**. الحكم للسيرفر —
   وشوف حدود ده في 5-ب1.
3. **التسجيل:** كل رفض أو علامة مشبوهة تتكتب في `attendance_security_logs`.
4. **الواجهات:** أي طريقة جديدة لازم تظهر في **أربع** شاشات اختيار (شركة/فرع/فئة/موظف) +
   في `ALLOWED` + في `check_in.php` و`check_out.php` — وإلا الـ resolver هيرجّعها والـ
   check-in هيرفضها. (وده بالظبط سبب الريفاكتور في القسم 4.)
5. **الصلاحيات:** بوابات القوائم/التابات في الواجهة لازم تطابق صلاحية كل endpoint، وإلا
   المستخدم قليل الصلاحية بياخد 403 بيظهر كـ"حدث خطأ".
6. **قانون 14/2025:** الطرق اللي بتجمع صور أو مواقع محتاجة موافقة صريحة موثّقة + سياسة
   احتفاظ (ملف كل عامل ≥ 5 سنين بعد انتهاء العلاقة). `photo_gps` بتتفادى الجزء البيومتري
   تحديدًا، **مش** الجزء الخاص بالموقع.
7. **الأرقام (E.164):** `Validator::phone` بيطلب `+<كود الدولة>` ويرفض الأرقام المحلية.
   القاعدة الحالية نضيفة (فُحصت)، **بس أعد الفحص قبل أي قناة بتطابق بالرقم** (واتساب/IVR/SMS)
   لو حصل استيراد كبير:
   ```sql
   SELECT COUNT(*) FROM employees WHERE phone REGEXP '^\\+[0-9]{1,3}0';   -- صفر محلي زيادة
   SELECT COUNT(*) FROM employees WHERE phone NOT LIKE '+%' AND phone<>''; -- من غير +
   ```

---

## 7) المستبعد نهائيًا

لوحات السيارات (ANPR) · الملبوسات (wearables) · UWB · التعرف على المشية (gait) · البلوك تشين ·
تحليلات CCTV بالذكاء الاصطناعي · القزحية/الشبكية · أوردة الكف · المساعد الصوتي · هندسة اليد.

يا بحثية يا هاردوير مش متوفر في مصر، والعائد التجاري دلوقتي صفر. تتراجع لو السوق اتغيّر،
مش قبل كده.

---

## 8) مرجع السوق — خلاصة مسح الطرق

> خلاصة `compass_artifact_wf-*.md` (2026-07-23). الملف الأصلي متساب كمرجع خام بتفاصيل كل طريقة.

**الخلاصة:** فيه أكتر من 30 طريقة عالميًا في 6 فئات. في مصر النهاردة **بصمة الإصبع** هي
الأكثر انتشارًا والأرخص (ZK LX50 ≈ 1,750 جنيه)، و**التعرف على الوجه** الأسرع نموًا خصوصًا
في المصانع، و**الموبايل بالـ GPS + سيلفي** هو الاتجاه الصاعد للفرق الميدانية والمندوبين.

| الفئة | الطرق | الوضع في مصر |
|---|---|---|
| بيومترية بأجهزة | بصمة، وجه، قزحية، أوردة الكف، صوت، هندسة اليد | البصمة والوجه منتشرين؛ الباقي نادر |
| كروت وتوكن | RFID، NFC، باركود/QR badges، بطاقات مغناطيسية | متوسط الانتشار |
| موبايل | GPS/geofence، سيلفي، QR، NFC tap، auto check-in | صاعد |
| شبكة | WiFi BSSID، IP، VPN، تسجيل دخول الكمبيوتر | ضعيف عند المنافسين المحليين |
| ناشئة | UWB، gait، blockchain، CCTV AI، wearables | شبه معدومة |
| يدوية/احتياطية | إدخال المدير، كشوف ورقية، Excel | لسه الأكثر استخدامًا في الشركات الصغيرة |

**الحقائق اللي بتحرّك السوق:**

1. **مفيش طريقة واحدة "أحسن"** — الاختيار بيعتمد على نوع الشركة: مصنع، مكتب، ريتيل، موقع
   إنشاءات، أو فرق ميدانية. ولذلك التجاوز على 4 مستويات ميزة مش تعقيد.
2. **buddy punching** هي المشكلة رقم 1 اللي بتحرّك السوق ناحية البيومتري والـ liveness.
   تقارير American Payroll Association (منقولة عبر Hubstaff/ShiftFlow) بتقول إنها بتأثر على
   **75% من الشركات الأمريكية** وبتكلّف ما يعادل **~2.2% من إجمالي الرواتب**.
3. **قانون العمل المصري 14/2025** — صدر 3 مايو 2025، سارٍ من **1 سبتمبر 2025**، والمحاكم
   العمالية المتخصصة من 1 أكتوبر 2025. بيلزم صاحب العمل بملف (ورقي أو إلكتروني) لكل عامل
   يُحتفظ به **≥ 5 سنين بعد انتهاء العلاقة**، وبينظّم العمل عن بُعد. يعني دقة تتبع الساعات
   **مسألة امتثال قانوني مش رفاهية**.
4. **الاتجاه العالمي contactless** بعد كوفيد — الوجه بقى مفضّل على البصمة اللي بيلمسها مئات
   الموظفين.
5. **ZKTeco وHikvision** مسيطرين على السوق المصري — علشان كده تكامل ADMS المباشر قرار تجاري
   مش تقني.

---

## تحذيرات على بيانات المنافسين

1. **كل أرقام وميزات المنافسين من موادهم التسويقية** أو مراجعات بتنقل عنهم. "بيدعم التعرف
   على الوجه" ممكن تعني liveness حقيقي أو مجرد صورة متخزّنة — Deputy مثلًا بيسوّق
   "Touchless Clock-In" واللي بيحصل فعليًا أقرب لتصوير + مراجعة بشرية.
2. **الخانات ⚠️ اجتهاد** — الميزة موجودة بشكل محدود أو في خطة أعلى أو عبر تكامل، مش أصلية.
3. **أرقام Nova HR** (1,000+ شركة، 50,000 مستخدم يومي) معلنة من الشركة نفسها ومش مستقلة.
4. **الجدول بيقدم بسرعة.** المنتجات دي بتطلع ميزات كل شهر — اعتبره صورة يوم 2026-08-02.

---

## المصادر

- [Jibble — Time Tracking Methods](https://www.jibble.io/collections/tracking-time) · [Kiosk](https://www.jibble.io/time-clock-kiosk-app) · [Geofencing](https://www.jibble.io/geofencing-attendance-system)
- [Connecteam — Time Tracking Methods](https://connecteam.com/employee-time-tracking-methods/) · [Kiosk](https://connecteam.com/employee-time-clock-app/time-clock-kiosk-app/) · [Geofence](https://connecteam.com/employee-time-clock-app/geofence-time-clock-app/)
- [ZenHR — Attendance & Time Tracking](https://www.zenhr.com/en/modules/attendance-and-time-tracking)
- [Bayzat — Attendance](https://www.bayzat.com/attendance)
- [Jisr — Attendance Management](https://www.jisr.net/en/attendance-management) · [PalmHR — Time](https://palmhr.net/en/products/time)
- [Nova HR — الحضور والانصراف ببصمة الوجه من الموبايل](https://novahrs.com/modules/attendance)
- [Buddy Punch review 2026](https://peoplemanagingpeople.com/tools/buddy-punch-review/) · [Deputy review 2026](https://connecteam.com/reviews/deputy/)
- [Workyard — Geofencing time tracking for construction](https://www.workyard.com/compare/top-geofencing-time-tracking-for-construction-projects)

**مراجع داخلية:** `specs/004-web-attendance-checkin/` · `specs/005-branch-kiosk/` ·
`backend_medjet/app/kiosk/README.md` · `backend_medjet/device/README.md` ·
`front_end/packages/medjat_shared/assets/models/README.md` (قياسات LFW والعتبة) ·
`compass_artifact_wf-*.md` (مسح السوق الخام)
