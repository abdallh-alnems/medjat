# طرق حضور مقترحة (مش موجودة في الخريطة الحالية)

> **مقترح — مفيش حاجة من ده اتنفّذت.** تاريخ: 2026-07-31.
> ملف مكمّل لـ `attendance-methods-implementable.md` (اللي فيه الطرق الشغّالة + الباقي من الخريطة الأصلية)
> ولـ `compass_artifact_*.md` (مسح السوق). الطرق اللي هنا **مش** موجودة في أي من الملفين.
>
> كل بند اتفحص مقابل الكود الفعلي، مش مقابل السوق بس.

---

## 0) ليه الملف ده أصلاً

الخريطة الحالية غطّت اللي المنافسين بيعملوه. الملف ده بيغطي **الفجوات**:

| الفجوة | مين واقع فيها | الوضع دلوقتي |
|---|---|---|
| عمالة بدون سمارت فون | أمن، نظافة، عمالة يومية، مصانع | **مقفولة تمامًا** — كل الطرق محتاجة تطبيق أو جهاز |
| مندوبين بيزوروا عملاء مش فرع | FMCG، أدوية، صيانة | `gps_only` بلا تحقق حقيقي (الـ geofence مربوط بـ `branches` بس) |
| طاقم ميداني بمشرف واحد | إنشاءات، مزارع | `manual` من غير إثبات موقع ولا صورة |
| شركات مش عايزة حمل البيانات البيومترية | أي شركة حاسبة لقانون 14/2025 | `face_selfie` أو لا حاجة |
| الـ QR الثابت بيتصوّر ويتوزّع | أي فرع بيستخدم `qr_gps` | ثغرة مفتوحة |
| الموبايل بيتعار للزميل | الكل | مفيش قفل جهاز للموظف |

---

## المجموعة أ — مكاسب سريعة (البنية موجودة، الجهد صغير) 🟢

### أ-1) QR متجدد زمنيًا بدل الثابت (`qr_gps` معدّلة) ⭐ الأولوية 1
- **المشكلة:** الـ QR المطبوع في الفرع أكبر ثغرة عملية — حد يصوّره ويبعته على واتساب، ويبقى كل زمايله
  بيسجّلوا من البيت (الـ geofence بيمسك الموقع، بس مش لو القاعدين في نفس المنطقة).
- **كيف تشتغل:** تابلت/شاشة في الاستقبال تعرض QR بيتغيّر كل 30 ثانية (نفس منطق الـ nonce أحادي الاستخدام
  اللي شغّال في `face_challenges`). السيرفر يتحقق إن الكود ما انتهاش وما اتستخدمش.
- **الحالة في الكود:** ✅ نمط الـ nonce موجود بالكامل — `face_challenges` (`nonce` + `expires_at` +
  `consumed_at` + `uniq_face_challenge_nonce`) و`app/attendance/face_challenge.php`. نفس الجدول بالظبط
  بس لـ QR.
- **المطلوب:** جدول `qr_challenges` (أو `purpose` جديد في الموجود) + endpoint يولّد الكود للشاشة +
  تحقق في `check_in.php` + صفحة عرض بسيطة (ممكن تكون صفحة في `medjat_central_web` تتفتح على أي شاشة).
- **مهم:** حساب انتهاء الصلاحية **في SQL** (`DATE_ADD(NOW(), INTERVAL ? SECOND)`) زي ما اتعمل في الوجه —
  PHP بيشتغل UTC والـ MySQL على توقيت السيرفر، فالكود بيتولد منتهي لو اتحسب في PHP.
- **الجهد:** صغير. **أعلى عائد لكل ساعة شغل في الملف كله.**

### أ-2) بيومترية الموبايل نفسه + قفل الجهاز (device binding) ⭐ الأولوية 2
- **المشكلة:** الموظف يدّي موبايله لزميله. ده الـ buddy punching الحقيقي في السوق، مش الصور المطبوعة.
- **كيف تشتغل:** حاجتين مع بعض:
  1. **`local_auth`** — بصمة/FaceID الموبايل نفسه كبوابة قبل زرار التسجيل.
  2. **قفل الجهاز** — الموظف مربوط بـ `device_id` واحد؛ أي جهاز تاني يتطلب موافقة أدمن.
- **الحالة في الكود:**
  - ✅ `employee_auth_tokens.device_id` موجود بالفعل — التوكن بيحمل الجهاز، بس **مفيش قفل**.
  - ✅ النمط اتنفّذ للأدمن بالفعل: `admins.active_device_id` + هيدر `X-Device-Id` (جهاز واحد، الأحدث يكسب).
  - ❌ `local_auth` مش في `pubspec.yaml` بتاع `medjat_app`.
- **المطلوب:** عمود `employees.active_device_id` + نفس منطق الأدمن + `local_auth` في التطبيق +
  مسار "طلب تغيير الجهاز" للأدمن (الموبايل بيضيع وبيتباع، لازم مخرج).
- **ليه ده مهم:** بيقفل الثغرة العملية **من غير موديل ML، ومن غير `mobilefacenet.tflite`
  (اللي لسه ناقص أصلاً)، ومن غير موافقة بيومترية مطلوبة قانونًا**.
- **الجهد:** صغير. **أرخص مكسب أمني موجود.**

### أ-3) صورة موثّقة بدون تعرّف وجه (`photo_gps`) — الأولوية 3
- **كيف تشتغل:** صورة + GPS + وقت، تتخزن **كدليل بس**. مفيش embedding، مفيش مطابقة، مفيش قرار آلي.
  المدير يشوف الصورة لو حصل خلاف.
- **ليه مطلوبة:** قانون العمل 14/2025 بيحمّل الشركة موافقة صريحة + سياسة احتفاظ للبيانات البيومترية.
  فيه شركات هتقول لأ لـ `face_selfie` لمجرد الحمل ده. `photo_gps` بتديها 80% من الردع بصفر التزام بيومتري.
- **الحالة في الكود:** ✅ رفع الصور وقت التسجيل موجود من مسار `face_selfie`. اللي محتاجه إنك **ما تشغّلش**
  `FaceMatchService`.
- **المطلوب:** إضافة `photo_gps` لـ `ALLOWED` + فرع في `check_in.php` بيخزّن الصورة ويعدّي +
  عرضها في شاشة حضور الفرع.
- **الجهد:** صغير.

### أ-4) حضور افتراضي بالجدول (exception-only) — الأولوية 6
- **كيف تشتغل:** الموظف المكتبي/الريموت يتحسب حاضر حسب ورديته في الجدول إلا لو اتسجّل استثناء
  (أجازة، مأمورية، غياب). مفيش تسجيل من الموظف أصلاً.
- **الحالة في الكود:** ✅ الجداول (`shifts` + الجدول الأسبوعي) موجودة، و✅ كرون الغياب المتأخر
  (`23:50` catch-up absences) بيعمل نص الشغل ده بالفعل.
- **المطلوب:** طريقة `scheduled` في `ALLOWED` + قلب منطق الكرون (افتراض حضور بدل افتراض غياب) +
  واجهة استثناءات للمدير.
- **الجهد:** صغير–متوسط. مناسبة للموظفين اللي التتبع معاهم مضايقة أكتر من فايدته.

---

## المجموعة ب — قنوات جديدة (بتفتح شرايح مقفولة) 🟡

### ب-1) تسجيل عبر واتساب (`whatsapp_gps`) ⭐ الأولوية 4
- **كيف تشتغل:** الموظف يبعت Location Pin على رقم الشركة على واتساب → webhook يستقبل الإحداثيات →
  نفس تحقق `GpsService` → check-in. الرد يرجع رسالة تأكيد.
- **ليه:** واتساب هو الواجهة الافتراضية في مصر. مفيش تطبيق يتنزّل، مفيش تدريب، مفيش مساحة تخزين.
  ممتاز للشركات الصغيرة اللي بتقاوم تنزيل تطبيق.
- **المطلوب:** WhatsApp Cloud API (Meta) + endpoint webhook في `app/attendance/` + ربط الرقم بالموظف.
- **⚠️ لازم يتراجع:** مطابقة الرقم هي أساس القناة دي. مسار الإدخال **اتظبط** بالفعل
  (`Validator::phone` بيطلب `+<كود الدولة>` صراحةً ويرفض الأرقام المحلية المجرّدة)، لكن فاضل حاجتين:
  **صفوف قديمة** اتعملت قبل الإصلاح ممكن تكون لسه فيها `+2001023809407`، و**الفاليديتور ما بيكشفش**
  الصفر المحلي الزيادة لو العميل بعت الرقم ملزوق كده أصلاً (بيعدّي من الـ regex). لازم استعلام تدقيق
  على القاعدة الحيّة + قرار: نصلّح الصفوف ولا نطابق بمرونة. (نفس الكلام على IVR والـ SMS.)
- **مخاطر:** الـ Location Pin في واتساب ممكن يتزوّر بنفس تطبيقات Fake GPS، و**مفيش عندك إشارة
  `is_mock_location`** في المسار ده — الحماية بتبقى أضعف من التطبيق. اعتبرها قناة للشركات اللي
  الثقة فيها مقبولة، مش للمواقع الحسّاسة.
- **الجهد:** متوسط.

### ب-2) مكالمة فائتة / IVR / SMS (للعمالة بدون سمارت فون) ⭐ الأولوية 5
- **المشكلة:** شريحة كبيرة في مصر (أمن، نظافة، عمالة يومية، ورديات مصانع) مالهاش سمارت فون.
  دي **مقفولة تمامًا** على مجات دلوقتي.
- **كيف تشتغل:** الموظف يعمل ميس كول على رقم الشركة من رقمه المسجّل → الـ caller ID يطابق الموظف →
  بصمة زمنية. بديل: SMS بكود الفرع.
- **المطلوب:** بوابة مكالمات/SMS مصرية + endpoint webhook + مطابقة الرقم (نفس عقبة الـ E.164 فوق).
- **قيود صريحة:** مفيش موقع، والموبايل يتعار. **أضعف طريقة أمنيًا في الملف** — لكنها أحسن من الورق
  اللي الشريحة دي بتستخدمه دلوقتي. تتقيّد بـ: نافذة زمنية ضيقة حوالين الوردية + مراجعة المشرف.
- **الجهد:** صغير–متوسط (التكلفة في البوابة مش في الكود).

### ب-3) تسجيل الطاقم بواسطة المشرف (`crew_gps`) ⭐ الأولوية 7
- **كيف تشتغل:** المشرف في الموقع يفتح قائمة طاقمه، يعلّم الحاضرين، ويتسجّل **GPS واحد + صورة جماعية +
  وقت** للمجموعة كلها.
- **الفرق عن `manual`:** `manual` بيتسجّل من مكتب الأدمن من غير أي إثبات. ده ميداني بإحداثيات وصورة
  ومسؤول محدد بالاسم.
- **المطلوب:** شاشة في `medjat_central` + endpoint دفعة (batch) + عمود يوضّح مين سجّل لمين
  (`recorded_by_employee_id`) عشان المسؤولية تبقى متتبّعة.
- **الجهد:** متوسط. سوق الإنشاءات والزراعة في مصر بيشتغل بالطريقة دي فعليًا.

### ب-4) تسجيل الدخول للكمبيوتر / إضافة متصفح / Slack-Teams — الأولوية 9
- مذكورة في ملف السوق (بنود 23 و24) و**ما اتنقلتش** للخريطة التنفيذية.
- **الحالة في الكود:** ✅ `medjat_central_web` (Next.js) جاهز يستضيف نقطة الدخول.
- **الأنسب لمين:** الـ BPO وشركات البرمجة (مصر مركز BPO كبير في MENA).
- **الجهد:** متوسط.

---

## المجموعة ج — تمايز تجاري (جهد كبير، عائد كبير) 🟠

### ج-1) Geofence لكل عميل/موقع مش لكل فرع (`visit_gps`) ⭐ الأولوية 8
- **المشكلة المعمارية:** الـ geofence بتاعك مربوط بـ `branches` (`latitude` / `longitude` /
  `gps_radius_meters`). مندوب بيزور 15 عميل في اليوم **مالهوش فرع** — يعني سوق المندوبين كله بيستخدم
  `gps_only` بلا أي تحقق حقيقي.
- **كيف تشتغل:** كيان "مواقع/عملاء" لكل شركة، بـ geofence لكل موقع، والموظف يتسجّل عند العميل.
  الحضور بيبقى سلسلة زيارات مش check-in/out واحد.
- **المطلوب:** جدول `sites` (أو `clients`) + ربط الموظف بخط سير + تعميم `GpsService` إنه يتحقق مقابل
  أي موقع مش الفرع بس + إعادة تفكير في شكل يوم العمل في `attendance` (زيارات متعددة).
- **الجهد:** كبير. **لكنه أكبر تمايز تجاري في الملف** — سوق الـ FMCG والأدوية في مصر ضخم ومحدش من
  المنافسين المحليين مغطّيه كويس.

---

## ملاحظة معمارية — لازم تتقرأ قبل إضافة أي طريقة ⚠️

`AttendanceMethodResolver::ALLOWED` دلوقتي **بيخلط الطريقة بالقيد**:

```php
ALLOWED = ['qr_gps', 'gps_only', 'face_selfie', 'wifi_gps', 'device', 'manual'];
```

`qr_gps` و`wifi_gps` و`face_selfie` كلها = **GPS + حاجة زيادة**، و`check_in.php` بيفرّع عليها بالاسم:

```php
if (!in_array($requestedMethod, ['qr_gps','gps_only','face_selfie','wifi_gps'], true)) { ... }
if ($requestedMethod === 'wifi_gps')    { /* NetworkVerifier */ }
if ($requestedMethod === 'face_selfie') { /* FaceMatchService */ }
```

لو ضفت الطرق اللي فوق، عدد التركيبات **بينفجر**: `photo_wifi_gps`؟ `nfc_face_gps`؟
`qr_rotating_wifi_gps`؟ العميل هيطلبها، وكل تركيبة = عنصر جديد في `ALLOWED` + فرع جديد في
`check_in.php` و`check_out.php` + خيار في 4 واجهات اختيار (شركة/فرع/فئة/موظف).

**البديل:** فصل المفهومين —

| المفهوم | الأمثلة |
|---|---|
| **طريقة** (إثبات إن ده أنا) | `self` (التطبيق) / `face` / `photo` / `whatsapp` / `phone_call` / `device` / `crew` / `manual` |
| **قيود** (شروط لازم تتحقق) | `gps` / `qr` / `wifi` / `rotating_qr` / `bound_device` / `local_biometric` / `schedule_window` |

الـ resolver يرجّع `{method, constraints[]}`، والـ `check_in.php` يلف على القيود ويطبّقها واحد واحد
بدل شجرة `if`. القيود تتخزن JSON على نفس مستويات التجاوز الموجودة (موظف ← فئة ← فرع ← شركة).

**التوقيت:** الريفاكتور ده يستاهل **قبل** ما تضيف تالت أو رابع طريقة، مش بعدها. ولو عملته، خلّي
القيم القديمة (`qr_gps` إلخ) تتقرأ وتتحوّل تلقائيًا — فيه تطبيقات في الستور لسه بتبعت الأسماء دي
(`check_in.php` فيه بالفعل fallback للبيلدات القديمة اللي مش بتبعت `method` أصلاً).

---

## جدول الأولويات المقترح

| # | الطريقة | المجموعة | الجهد | الشريحة اللي بتفتحها |
|---|---|---|---|---|
| 1 | QR متجدد زمنيًا | أ | صغير | كل مستخدمي `qr_gps` (سد ثغرة) |
| 2 | بيومترية الموبايل + قفل الجهاز | أ | صغير | الكل (سد ثغرة) |
| 3 | صورة بدون تعرّف وجه (`photo_gps`) | أ | صغير | الشركات المتحفظة على البيومتري |
| 4 | واتساب | ب | متوسط | الشركات الصغيرة الرافضة لتطبيق |
| 5 | ميس كول / IVR / SMS | ب | صغير–متوسط | العمالة بدون سمارت فون |
| 6 | حضور افتراضي بالجدول | أ | صغير–متوسط | المكتبي والريموت |
| 7 | تسجيل الطاقم بالمشرف | ب | متوسط | الإنشاءات والزراعة |
| 8 | `visit_gps` (geofence لكل عميل) | ج | كبير | المندوبين — FMCG وأدوية |
| 9 | Desktop / متصفح / Slack | ب | متوسط | الـ BPO والبرمجة |

**ترتيب التنفيذ المقترح:** 1 → 2 → 4 → 7 → 8
(الاتنين الأولانيين بيقفلوا ثغرات في اللي شغّال دلوقتي؛ الباقي بيفتح أسواق جديدة.)

---

## متطلبات مشتركة قبل أي تنفيذ

1. **تدقيق أرقام التليفون (E.164):** مسار الإدخال بقى سليم — `Validator::phone` (`core/Validator.php:44`)
   بيطلب `+<كود الدولة>` ويرفض الأرقام المحلية المجرّدة، وبيتنادى من `employees/create.php`
   و`employees/update.php` و`auth/update_profile.php`. الباقي: **صفوف قديمة** اتعملت قبل الإصلاح
   ممكن تكون فيها صفر محلي زيادة (`+2001023809407`)، والـ regex ما بيرفضهاش لو وصلت جاهزة كده.
   لازم استعلام تدقيق على القاعدة الحيّة قبل ما تبني أي قناة بتطابق بالرقم (واتساب، IVR، SMS).
2. **التوقيت:** أي طريقة جديدة تكتب وقت لازم تعدّي على `core/TenantClock.php`، مش `date()` ولا `NOW()`.
   وحسابات الانتهاء/الصلاحية **في SQL** مش في PHP.
3. **الأمان:** أي إشارة جاية من العميل (موقع، "أنا اتحققت") **مش مصدر ثقة**. الحكم للسيرفر دايمًا —
   نفس القرار اللي اتاخد في `face_selfie` عن قصد.
4. **التسجيل:** كل رفض أو علامة مشبوهة تتكتب في `attendance_security_logs` (الجدول اتعمل على القاعدة
   الحيّة 2026-07-31).
5. **الواجهات:** أي طريقة جديدة لازم تظهر في **أربع** شاشات اختيار (شركة/فرع/فئة/موظف) + في
   `AttendanceMethodResolver::ALLOWED` + في `check_in.php` و`check_out.php`، وإلا الـ resolver
   هيرجّعها والـ check-in هيرفضها.
6. **قانون 14/2025:** الطرق اللي بتجمع صور أو مواقع محتاجة موافقة صريحة موثّقة + سياسة احتفاظ
   (الملفات ≥ 5 سنين). `photo_gps` بتتفادى الجزء البيومتري تحديدًا، مش الجزء الخاص بالموقع.

---

## المستبعد (اتفاقًا مع الملفين التانيين)

لوحات السيارات (ANPR)، الملبوسات (wearables)، UWB، التعرف على المشية، البلوك تشين، CCTV AI،
القزحية، أوردة الكف، المساعد الصوتي — يا بحثية يا هاردوير مش متوفر في مصر، والعائد التجاري
دلوقتي صفر. تتراجع لو السوق اتغيّر، مش قبل كده.
