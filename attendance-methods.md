# طرق الحضور — إيه المعمول وإيه اللي لأ

**تاريخ: 2026-08-14**

> **المنهج:** كل صف عن مجات **متحقَّق من الكود ومن السيرفر الحيّ** يوم 2026-08-14.
> كل صف عن السوق من مسح السوق (2026-07-23) ومقارنة المنافسين (2026-08-02) —
> ودول من **مواد الموردين التسويقية**، اقراها كـ"بيعلنوا إنهم بيعملوا كده" مش قياس مستقل.

---

## الخلاصة في ٣ سطور

1. عالميًا فيه **٣٤ طريقة موثّقة**. مجات مغطّية **١٦** منها (١٥ كاملة + الـliveness جزئية)، وعندها **٢** مش في القايمة العالمية أصلاً.
2. مقابل المنافسين الإقليميين (ZenHR · Nova HR · Bayzat): **صفر فجوة**. مقابل العالميين: **٤ بنود**، الجدّي فيهم واحد.
3. **الفجوة الحقيقية الوحيدة مش طريقة ناقصة** — هي كشف الحياة **جوّه طريقة شغّالة بالفعل** (`face_selfie`).

---

## ١) اللي معمول ✅

### ١.١ الطرق — `AttendanceMethodResolver::ALLOWED` (٩ عناصر)

```php
['qr_gps', 'gps_only', 'face_selfie', 'photo_gps', 'wifi_gps', 'crew_gps', 'device', 'manual', 'kiosk']
```

| الطريقة | بتغطّي إيه عالميًا | نقطة الدخول |
|---|---|---|
| `qr_gps` | #11 باركود/QR badges · #15 مسح QR بالموبايل | `check_in.php` → `GpsService` (+ QR متجدد كل ٣٠ث) |
| `gps_only` | #12 GPS · #13 geofencing | `GpsService::validateCheckIn` — نصف قطر لكل فرع |
| `wifi_gps` | #17 التحقق بالـIP · #18 WiFi BSSID | `NetworkVerifier::verify` — BSSID/IP/CIDR + وضع تعلّم |
| `face_selfie` | #14 التعرف على الوجه بالموبايل | `FaceMatchService::verify` — **المطابقة على السيرفر** + nonce |
| `photo_gps` | صورة كدليل من غير مطابقة ولا قرار آلي | نفس المسار من غير `FaceMatchService` |
| `crew_gps` | المشرف يسجّل لطاقمه: GPS + صورة + `recorded_by` | `crew_check_in.php` · `crew_list.php` |
| `device` | **٥ طرق عالمية** — تحت | `device/iclock.php` → `DevicePunchIngestor` |
| `kiosk` | #19 multi-factor · #31 PIN · #14 وجه 1:N | `app/kiosk/*` — قاعدة هامش + PIN + كود |
| `manual` | #32 إدخال/اعتماد المدير | `manual_check_in.php` |

**`SELF_SERVICE`** (اللي الموظف يسجّل بيه لنفسه) = `qr_gps` · `gps_only` · `face_selfie` · `photo_gps` · `wifi_gps`.

### ١.٢ الترمينال لوحده بيغطي ٥ طرق عالمية

`ZktecoAdms::VERIFY_MODES` بيترجم اللي الجهاز بيبلّغه:

| verify | بيتخزن كـ | الطريقة العالمية |
|---|---|---|
| 1, 2 | `device_fingerprint` | **#1 بصمة الإصبع** — الأكثر انتشارًا في مصر |
| 15, 16 | `device_face` | **#2 التعرف على الوجه بجهاز** |
| 3, 4 | `device_card` | **#7 RFID** · **#9 كروت التقارب** (+ `device_users.card_number`) |
| 0 | `device_password` | **#31 PIN** |

يعني الكروت والبصمة **شغّالة النهاردة**، مش فجوة. ومعاها `import_punches.php` + `PunchCsvParser`
لو مفيش بورت مفتوح، وقارئ على الشبكة المحلية من تطبيق الديسكتوب (`528e016`).

### ١.٣ القنوات — **مش** نفس مفهوم الطريقة

| القناة | مين بيوثّق | الطريقة العالمية |
|---|---|---|
| تطبيق الموظف | `X-Employee-Token` | — |
| المتصفح | جلسة ويب عبر بروكسي Next.js | **#34 بوابة ذاتية على الويب** (+ صورة إجبارية اختياريًا) |
| الكيوسك | `X-Kiosk-Token` — بيوثّق كـ**فرع** مش كشخص | — |
| الترمينال | السيريال + ADMS | — |

القناة بتيجي من **الجلسة الموثّقة** مش من الطلب (`check_in.php:21-25`) — متصفح ما يقدرش يتنكّر في تطبيق.

### ١.٤ حاجتين عندك مش في القايمة العالمية خالص

- **`photo_gps`** — دليل من غير التزام بيومتري تحت ١٤/٢٠٢٥.
- **`crew_gps`** — تسجيل ميداني بإحداثيات وصورة ومسؤول بالاسم.
- (وكمان **BSSID بوضع تعلّم** و**التجاوز على ٤ مستويات** — تفاصيل مالهاش نظير عند حد.)

---

## ٢) اللي مش معمول ❌

### ٢.١ الفجوة الوحيدة اللي تستاهل: **كشف الحياة (#30)**

المسح العالمي كاتب حرفيًا: *«بقى **متطلب أساسي في 2026** مش ميزة كماليّة»*، و*«**٤٢٪** من الأنظمة القديمة بتتخدع بصورة مطبوعة»*.

انت **بتبيع** `face_selfie` — بس الـliveness **بوليان جاي من العميل**:

```php
$livenessPassed = !empty($input['liveness_passed']);                 // FaceMatchService.php:215
$candidate = self::parseEmbedding($input['face_embedding'] ?? null); // :228 — ١٩٢ رقم من العميل
```

**السيرفر ما بيشوفش الصورة أبدًا.** فبيلد معدّل يبعت أرقام مخزّنة + `liveness_passed: true` والكاميرا ما فتحتش.
نفس الملاحظة على `app/kiosk/identify.php`.

**اللي اتعمل:** كشف تكرار الـembedding ✅ (`2026_08_06_face_replay_detection.sql`).
**اللي لسه:** **Firebase App Check معطّل** — `firebase_app_check: ^0.3.2` في `pubspec.yaml` و`grep` بيرجّع صفر استخدام. انت دافع تمنها.

> ده مش "طريقة ناقصة" — ده **معيار عالمي انت تحته جوّه طريقة بتبيعها**. أهم بند في الملف.

### ٢.٢ بتتستخدم فعلًا عالميًا ومش عندك (١١)

| # | الطريقة | مين بيستخدمها | يخصّك؟ |
|---|---|---|---|
| 23 | **تسجيل دخول الكمبيوتر** | WorkTime · TimeTac | ✔ **مصر مركز BPO كبير** |
| 24 | **إضافة المتصفح** | Clockify · Apploye · HiveDesk | ✔ فريلانسرز ووكالات |
| 10 | **NFC** | Jibble · Connecteam · Where's My Staff (إقليمي) | ~ بديل للـQR بنفس الأمان |
| 4 | **أوردة الكف** | Fujitsu PalmSecure | ✔ **متاح في مصر: ZK MB20 ~٣,٩٩٨ ج** |
| 22 | **لوحات السيارات (ANPR)** | logistics yards · campuses | ✗ |
| 28 | **CCTV بتحليلات AI** | viAct · Vizenta | ✗ ناشئ + مخاوف خصوصية |
| 16 | **Bluetooth Beacon** | OpenTimeClock | ✗ نادر في مصر |
| 20 | **الملبوسات (Wearables)** | Agrigistics | ✗ |
| 3 | **القزحية** | بنوك · datacenters | ✗ |
| 21 | **UWB** | مستودعات ومصانع ضخمة | ✗ غير موجود في مصر |
| 27 | **الكشف الحراري** | بييجي مدمج مع تيرمينالات الوجه | ✗ |

### ٢.٣ legacy بتموت — **متلحقهاش** (٣)

**#6 هندسة اليد** (بتنحسر عالميًا لصالح الكف والوجه) · **#8 الكروت الممغنطة** (سهلة النسخ، بتختفي) · **#33 OCR للكشوف الورقية** (حل انتقالي).

### ٢.٤ محدش بيستخدمها — بكلام المسح نفسه (٤)

| # | الطريقة | نص المسح |
|---|---|---|
| 5 | التعرف على الصوت | *«شبه معدوم في الحضور العملي»* |
| 25 | المساعد الصوتي | *«تجريبي جدًا، نادر عالميًا»* |
| 26 | التعرف على المشية | *«لسه بحثي غالبًا»* |
| 29 | البلوك تشين | *«بحثي غالبًا، overkill لأغلب الشركات»* |

---

## ٣) مقابل المنافسين

### ٣.١ الإقليميين — **صفر فجوة**

**ZenHR · Nova HR · Bayzat: ولا طريقة واحدة عندهم ومش عندك.** والعكس صح في ٦ حاجات:
BSSID بوضع تعلّم · المطابقة على السيرفر بـnonce · ZKTeco مباشر (ZenHR عبر BioTimeCloud، Bayzat عبر شركاء) · التجاوز على ٤ مستويات بـunion · طابور offline · توقيت لكل شركة **بالاسم مش بأوفست**.

### ٣.٢ العالميين — ٤ بنود

| البند | مين عنده من الـ٨ | الحكم |
|---|---|---|
| **الربط بمشروع/أمر شغل** | Jibble · Connecteam · Deputy · Buddy Punch (**٤**) | الجدّي الوحيد — **صفر من الإقليميين** |
| NFC | Jibble · Connecteam (**٢**) | مكسب أمني ~صفر طالما `qr_gps` متجدّدة |
| دخول/خروج تلقائي | Connecteam ✅ · Jibble ⚠️ (**١.٥**) | `'auto'` موجودة في الـenum ومحدش بيكتبها |
| تتبّع المسار أثناء الوردية | Connecteam ✅ · Jibble ⚠️ (**١.٥**) | ⛔ **مستبعدة بقرار** — حسّاسة تحت ١٤/٢٠٢٥ |

⚠️ الجدول ده بيثبت التعادل في الصفوف اللي **حد اختار يكتبها**، مش إن مفيش حاجة برّه.

---

## ٤) بنود مفتوحة **مش طرق** أصلاً

| البند | إيه هو |
|---|---|
| **الربط بمشروع/أمر شغل** | بُعد ناقص في البيانات: "بيشتغل على إيه" مش "مين وفين". بيغيّر `attendance` + التقارير + الـpayroll |
| **`visit_gps`** | الـgeofence مربوط بـ`branches` بس — مفيش `sites`/`clients`. سوق المندوبين شغّال `gps_only` بلا تحقق. **مفيش منافس عنده** |
| **سطر الكف في `VERIFY_MODES`** | ٦ أكواد بس + `?? 'device_fingerprint'`. عميل بجهاز MB20 هيتسجّل عادي بس **بلابل غلط**. سطر واحد |
| **`'auto'` في `check_out_method`** | القيمة موجودة (`schema.sql:237`) وصفر writer |
| **الدين المعماري** | `ALLOWED` بتخلط **الطريقة** بالـ**قيد** وبقت ٩ عناصر. الفصل يسبق أي إضافة جديدة. البديل: الـresolver يرجّع `{method, constraints[]}` و`check_in.php` يلف على القيود بدل شجرة `if` — القيود (`gps`/`qr`/`wifi`/`rotating_qr`/`bound_device`) تتخزن JSON على نفس مستويات التجاوز. خلّي القيم القديمة تتحوّل تلقائيًا: فيه تطبيقات في الستور لسه بتبعتها |

---

## ٥) قنوات مقترحة — مفيش منافس عنده أي واحدة فيهم

**واتساب (`whatsapp_gps`)** · **ميس كول / IVR / SMS** — دول **شرايح مقفولة** مش فجوات تنافسية:
الأولى بتخدم شركة مش عايزة تنزّل تطبيق، والتانية بتخدم عامل مالوش سمارت فون.
⚠️ الاتنين **أضعف أمنيًا** من الموجود (مفيش `is_mock_location` في مسار واتساب، ومفيش موقع خالص في الميس كول).
**متبنيش أي واحدة فيهم قبل ما عميل دافع يطلبها بالاسم.**

---

## ٦) الترتيب الموصى بيه

1. **فعّل Firebase App Check** — مدفوع تمنه ومعطّل، وبيحمي ٩ طرق شغّالة بدل ما يضيف عاشرة.
2. **سطر الكف في `VERIFY_MODES`** — دقايق.
3. **الريفاكتور (طريقة vs قيد)** — قبل أي طريقة جديدة، مش بعدها.
4. **الربط بمشروع** أو **`visit_gps`** — أول ما عميل يطلب. دول شرايح، مش تعادل.
5. **الباقي** — متعملش.

---

**مراجع:** `specs/004-web-attendance-checkin/` · `specs/005-branch-kiosk/` ·
`backend_medjet/app/kiosk/README.md` · `backend_medjet/device/README.md` ·
`frontend/mobile/shared/assets/models/README.md` (قياسات LFW والعتبة)

**الملف ده بديل لملفين اتمسحوا 2026-08-14:** `attendance.md` و`compass_artifact_wf-*.md`.
آخر نسخة منهم في **`528e016`** — استرجاع: `git show 528e016:attendance.md`.
اللي **ما اتنقلش** لهنا ولسه في التاريخ بس: البوست-مورتم بتاع تصادم ميجريشن إنيوم
`attendance_security_logs` · قرار بورت 8090 وتكلفته · الـ٧ قواعد قبل أي طريقة جديدة ·
القرارات المسجّلة (WebAuthn مرفوض، الروت متعمّد إنه ما يتحجبش).
